---
title: "79_マルチエージェントでナレッジグラフ構築 Neo4j×Google ADKコース（出典）"
tags: [raw-source]
source: DeepLearning.AI コース（Neo4j × Google ADK）
author: Andrew Ng × Stennett Elliott（Neo4j Developer Evangelist）※
published: 2026-08-04
created: 2026-08-04
---

# 出典メタデータ

- ソース: DeepLearning.AI 短期コース「Building a Multi-Agent System to Construct a Knowledge Graph」（Neo4j × Google ADK）
- 講師: Andrew Ng（DeepLearning.AI）× **Stennett Elliott**（Neo4j Developer Evangelist for GenAI）※ASR「on this colleague」は推定で Stennett Elliott
- 貢献者: Martin O'Hanlon・Adam Kelly（Neo4j/DBAI）、Rosalind
- 公開/取得: 2026年8月4日
- 形式: 文字起こし（`20260804_103340_transcription.txt`・ユーザー提供・WSL上で生成）
- 技術スタック: Google ADK（Agent Development Kit）・Neo4j・LiteLLM・OpenAI GPT-4o・Cypher
- ※自動文字起こし。話者ラベルなし・「NewFJ」=Neo4j、「A. D. K.」=ADK、「Light LLM」=LiteLLM、「four is」=4o 等の誤変換が想定される

---

# 文字起こし（全文）

In this course, we'll design a multi-agent system that transforms your structured and unstructured data into a knowledge graph. A knowledge graph is really useful for high-stakes applications where accuracy of the information we store and retrieve is important. This course will teach you how to give great set of tools for building these knowledge graphs. On the lighted the structure for this course is on this colleague who is a developer evangelist for Gen Z AI at Neo4j. Thanks, Andrew. I'm excited to be back here to work with you on this course. Similar to how a knowledge graph system splits text documents into chunks and stores them in a graph database, but in addition to that, it places chunks into graph structure, which chunks is later entities extracted from the chunks. For example, a text chunk from a product review can contain entities such as product orders or the issues or product defects. The knowledge graph extracts relevant entities from the chunks, then connects the chunks and entities in a graph. For edges, here each edge represents a relationship. For example, maybe represents this chunk mentioned as a product or that product has an issue. The entities are retrieved alongside the relevant chunks to provide more relevant context to the um to generate more precise and accurate answers. You can also connect this graph to another graph containing additional information extracted from structured data such as CSV files. In this course, on this will walk you through how to build a multi-agent system that helps you do all the work of constructing this type of knowledge graph to transform your structured and unstructured data into a knowledge graph. You first need to decide on the graph schema, meaning what types of entities or nodes you can extract from the data and what relationships exist between them. Once you have the schema defined, you can construct the actual graph and store it in a graph database. Instead of manually looking at your data to find the graph schema, you'll design a multi-agent system for doing that using Google's A. D. K. Or Agent Development Kit. After you learn the basic syntax with A. D. K. , you'll design a system one agent at a time. The first agent will converse with you to extract the goal and type of graph you want to build. Then, given that goal, a group of agents will specialize in extracting the entities and relationships from your structured data, and another group of agents will handle your unstructured data. Finally, a final set of agents will connect the two models and construct the graph accordingly. Many people have worked to create this course. I'd like to thank from Neo4j, Martin O'Hanlon, and Adam Kelly from DBAI for their support throughout the course and how Rosalind also contributed to the course. In the first lesson, you'll learn about the underlying structure of knowledge graphs and how you can build one and use it to identify causes of product issues. Sounds great. Let's get started. Through what a knowledge graph means and how they help represent and retrieve relationships from your data, you'll explore the data set you'll work on to build a knowledge graph. Let's dive in. So, what is a knowledge graph?

We're going to take a step back from it and reflect on relational databases and what a relational schema looks like. We all probably are familiar with some scenario like this, where there'll be some table on the left and some table on the right, and this table in the middle that is the join table that allows for multiple connections between the persons and the products in this particular example. So, think about these two tables with the join table. If you ask a question like what products has this person purchased, you'd first start with the person table and select this person here to identify A. B. K. And of course, you'd join it with the person-product table, and you'd see that the person-product table has a join between A. B. K. And some chair, A. B. K. And a desk, and desk. One more join across from the person to the person-product to the product table. Now you've got a complete answer. Where from A. B. K. You can see that here's the products that A. B. K. Has purchased this chair, lamp, and desk. You can get details about those purchases. What if you extended that question?

And you asked who else bought products that A. B. K. Bought?

Well, you start with the same join, going from A. B. K. To the products, and then you join back to the person-product table, and from there back to the person table, and now you'd get this person E. E. Who has bought some products that A. B. K. Also bought. But notice that E. E. Is also bought one extra product. Hmm. That's interesting. So we could probably extend this one more step and ask, well. What product should we recommend that ABK buy next?

Now this is kind of the basic of the recommendation query. If you've got some purchase pattern from ABK that matches a purchase pattern from some other person or other group of people, the thing you want to recommend to ABK is what that other group of people bought that ABK has not. So the same joins apply from ABK through to the products back to the other people that have bought those products. There's two joins. It's getting a little confusing. There's actually a nicer way to actually think about this and to query this. Let's turn this into a graph. The first step is to just draw all the records that don't matter, all those records that weren't part of the original query. Let's put those aside for the moment and focus on the records that we know about that are part of the dataset so far that matters for this query about recommending something to ABK. Get rid of all the join table records in the middle. Turn those into arrows connecting ABK directly over to the products he's purchased, and the same thing for EE over to the products that they've purchased. And now just rearrange the records a little bit. You've got ABK on one side, EE on the other side, and now looks much nicer. It's much easier to understand what's going on. Both ABK and EE are connected to these products in the middle, but then also this person EE is connected to another product that has no connection to ABK. You can start to turn this into a query by rephrasing it as a pattern match. Starting with ABK on the left side, you can describe a query in a query language called Cypher, where it looks a little bit like SQL if SQL had pattern match abilities. So here we're going to describe a pattern that describes the data records that you want to find. We're going to match from the data record that we're going to call ABK that has a label of person and that has a property with a name key where the value of it is ABK. So that's in round parentheses. We know that that's a node in graph terms, and then we kind of have this right-facing arrow with colon purchased in there. That's a relationship type that is purchased. So match a pattern where ABK purchased over to some other purchases that we're going to call ABK products. That's going to be a pattern match from ABK to the products that ABK matched. We can return those directly, and that's going to be the products in the middle there. We can extend that pattern to include the other people who bought those products, since we went from ABK to the products that ABK purchased. We're going to also have relationships going in to those products and say, okay, there's some other people who bought those products. This pattern will find all of the products that people have in common with ABK. We can extend it a little bit more to ask that recommendation question problem, where instead of saying we just want to find these other products, the other products have a unique property where they're actually something that ABK does not have. So we have the same pattern match from ABK to some products to some other people, and then extend that to other products, and now include a predicate saying we actually want a negative pattern to be true that ABK has not purchased those other products. So we're not ABK purchased those other products. So down there in the bottom that dotted line from ABK to the couch, we want that to not exist. Now we're describing a pattern that both has records that should exist, something that should not exist. The other products now will be reduced to only products that ABK has not purchased. That's what we're going to use as a recommendation to buy. So what really is a knowledge graph?

It's a kind of database that represents information as nodes representing things like people, products, blogs, whatever you might want to have as records in a database, but then also relationships that aren't simply conventionally using a join table, but that are first-class citizens. They're actually data records inside of the database that have semantic meaning. They actually represent how two nodes are connected and they add information about those two nodes. So both the nodes, relationships have key-value properties. They have multiple labels on nodes. Relationships always have a direction and they have a single type. And the query language that you use for doing pattern matching is called Cypher. And it turns out that this is super nice for mapping to natural language and really convenient for working with generative models because all of them are really good at natural language. We go back from here and just look at that query at the top. You can read that out loud and make sense of it. Match ABK who is a person whose name is ABK that bought some products that some other people bought. They bought some other products where ABK has Combine both vector similarities and pattern matching together for all kinds of powerful action patterns. So, for example, if you wanted to do root cause analysis, and you imagine that you're a furniture manufacturer, looking to understand customer complaints about your products, so you turn to your team of experts, you know, data engineering, data analysis team, and say, "Hey, could you do some root cause analysis?

We have a lot of complaints out on the internet about our products. Let's try to figure out what's going on." So, root cause analysis would include questions like, "Which products have the most issues?

" And then, based on those products, well, what part of the product is the actual problem?

And then you might want to ask the question like, "Is there a problem with the product itself?

Perhaps there are other things that are going on. People just don't like the product." But if you can take corrective measures, be because there's something that's in the manufacturing process, what you're looking for here is for corrective measures that you can take, do root cause analysis for something you can improve. So, if it's a design problem, you could turn that over to the design team. They could take a look at what people are complaining about. But if it's a manufacturing problem or a problem with the part somewhere, you can do some analysis on that, find the part problem, and either swap out the part for a new one or figure out how to improve the process itself. So, as a data engineer, if you're handed this task, you start with the usual thing: ask some clarifying questions. Why are we really doing this?

You want to be able to understand that there's possible manufacturing problems in the supply chain. Cool. Okay. Second question is, all right, if that's what we want to find out, what data is available that we can do this analysis with?

So, turns out in this scenario, we've got the material some CSV files that connect the product all the way to the suppliers from those products. Maybe it came from some spreadsheets, maybe it came from some relational database that isn't really suited for doing analysis. That's okay too. And you also have some data from user reviews that you scrape from some websites on the internet. You want to be able to combine those into one dataset that you can then do some analysis on. So, to do that analysis, you're going to build an all-graph that connects all the data. So, here's what the target data model is going to look like. On the left, in the sort of beige boxes over there, you see the CSV files that are available. Each one of those is going to turn into nodes in the graph, or relationships in the graph. That's going to be the kind of what we're going to call the domain graph. On the right side, on the bottom, there you also see some beige boxes that represent markdown files. Those markdown files are going to be chunked up. They're going to create some documents inside the graph as well. And then all of this is going to get connected. We'll look at some of the details as we actually go through doing the agent descriptions themselves. But what I want to call out here right now is that these are really very connected, but also very distinct parts of the graph. We've got the data sources, the structured data on the one side there. That's going to turn into what we call the domain graph. So that's the structured data that we know that we can query with pattern matching. On the other side, the unstructured data sources are not going to be queried in the same way, and they're not going to be imported in the same way either. So they're going to turn into what we call the lexical graph, which represents the original textual data, but also the structure around that textual data to connect those two pieces: the unstructured data and the structured data. We've got this part in the middle that we're going to call the subject graph. The subject graph is going to be subjects or entities that we pull out of the chunks. So the chunks will have some text. We'll be talking about products, maybe they'll be user names and things. All of these things might be entities. We're going to find those, call those things subjects. Those subjects are going to be connected to objects. So the subjects talk about some things. So some user, let's say that it's me, user A B K, loves this table. And so it'll be a subject predicate object in the subject graph that talks about A B K loving some table. That also, because A B K loves the table, the table can correspond with the product that we have in the structured data. If A B K mentions a table, and the table is a product that exists in the structured data, we can then connect the entities that we've extracted from the text all the way over to Intelligent behavior that's happening because of the LLM call, but the execution of that intelligent behavior is still happening through code. What's great about this architecture, of course, is that agents are really powerful because you've got an LLM there doing reasoning and call out tools that can do anything you can do with code. It's adaptive because the LLM is given memory, so it can learn what's happened either through conversation or through things that you save into memory that are important for it to decide future outcomes. And it's pretty easy to get started because you're really just doing natural language descriptions through the prompts about what the agent should do. But all this power and ease comes with some downsides. It can be slow working with an agent because the calls out to remote LLMs is a bit expensive and slow. It's also non-deterministic, well, because LLMs are non-deterministic, and you know token costs over time can really add quite a lot. So if you're running an agent over time, in production for thousands, thousands of calls, the token costs get really expensive really quickly. That's one of the advantages of moving to a multi-agent system is that you can actually prove some of the downsides here. So what is exactly a multi-agent system?

It is, of course, just a system where you have multiple agents working collaboratively on a single goal. The agents typically are arranged into a kind of a hierarchy where you have a top-level agent that manages the overall flow, and then as many sub-level agents as you like to that have either different phases of the work or very specific tasks that they're performing. The agents interact in a couple different ways. There's a main conversational thread that's basically happening with the user, where the user might have some message that they kick off the work with. Then each of the agents can decide is this work that I can do or something that somebody else should do, so kind of delegate this to a different agent. So you can see this illustration here that the root agent is able to delegate to either Agent A or Agent B. Internally, Agent A and Agent B can do some work using the tools that they've been given. One of the tools in Agent B is unique. This P agent here, this P function, is actually going to be in Agent itself, but being called as its work tool. So there's two ways for tools to interact: either agents delegating to each other, or an agent that's behaving as a tool being called by another agent. And along the way, you've got specific checkpoints with the user that are possible whenever you have any of these transitions. So what is the multi-agent system that you'll build?

You'll build parts of a knowledge graph agent, focusing on the sub-agents that perform knowledge graph construction. You'll learn the basics of building an agent using Google's ADK. You'll learn what memory is and how to use it for recording critical information. You'll define some tools, and of course, all of this will convert from open-ended conversation to executable code. This beautiful diagram are all the agents and the flow of agents and their interactions. Let's walk through this a little bit at a time. At the top level, this is the agent that manages the overall introduction of the user to what's possible, what the agent's responsibility is, and helping the user understand what's the workflow that's possible here from knowledge graph construction all the way to doing graph retrieval. So this is really a conversational agent that itself is not going to do any work, but it's going to guide the user through different phases of work that are going to occur. It's this next level of agents down that are really going to manage the individual workflows. And there are three major workflows. It's really on the left. We've got two workflows: the structured data agent and the unstructured data agent. These are responsible for taking the user through the idea of like what they might want to do all the way to constructing a graph. And then if those have done their job, the third agent, the graphrag agent on the right side, is responsible for helping the user ask questions using that graph. But the structured data agent and the unstructured data agent are workflow agents. They perform their work by interacting with the user and helping them go through the steps of what needs to happen from the idea of what they want to do all the way to describing the graph. And they do that by delegating to sub-agents that specialize in each phase of the workflow. You'll see some replication here that both the structured and unstructured data have some agents that are common, and I'll touch on those as we get into the one level down. Okay, so here's the sub-agent's actually doing the workflow for structured data. Let's walk through these one at a time, and also, what's important to note here is the output of actually these agents doing their work. If you The direction overall of what the job is. So this user intent agent, even though it's collaborative and kind of conversational, the output of this is really important. We're capturing what is the user goal, what are they trying to get out of this work. Based on that user goal, then the next agent down the file suggestion agent is going to take into account what's been established to the user intent agent as the direction and the goal of what the work is. And then it's going to look at the data files that are available. Try to find from those data files which ones are useful for achieving that goal. There might be more files that are available. There might be even multiple data sources that are available. Here, we're simplifying it down to just making suggestions for files that are available on disk. The output of that ends up being a list of suggested files that have been approved by the user. And then that gets input into the next agent, which is the schema proposal agent. Schema proposal agent is actually a pair of agents formed in a critic pattern, where one of the agents is responsible for making a proposal about what the schema should look like, and the next one is going to be a critic saying, well, maybe that's not quite so right. Think about changing this way or that way. We'll look at the details in the later lesson. But the idea here is that this pairing of agents internally will loop through what's possible, self-criticizes itself, and the outcome of that to be a good graph schema that uses the files that were approved from the previous agent and also in compliance with user goals that were set by the first agent, producing a graph schema that can answer questions that are useful for the user. So the output of all of that, we're calling a graph construction plan. It's not a graph itself, but it's a description about how to construct that graph. The second workflow, the first two parts of that workflow, are exactly the structured data workflow. Here in the middle, we're dealing with unstructured data, but it still begins with understanding what is the user intent for bringing in the unstructured data, and here that's markdown files. And then, okay, if you understand why the user wants to bring in markdown files, you're going to identify which files are relevant. Again, a file suggestion agent would do that, similar behavior to the structured data, but this third step is different. In the third step, rather than designing a schema based on some specific files, all you have is text, and from that text, how do you create a graph?

Well, the approach here is that we're going to have two specialized agents that look through the text and identify or call entities, so people, places, and things that are covered in the text, and also for those entities, identify what facts are describing the entities within the text. So let's say that was, you know, I was in the text and I had some reviews of local coffee shops. It might reveal that, you know, A B K really loves to score coffee from fills, because who doesn't?

So those facts could be extracted from the text. The goal of this agent is actually to find out what kinds of facts can be extracted, and not do the extraction, but simply describe what's possible. And so that we're going to be calling a knowledge extraction plan. The knowledge extraction plan paired with the graph construction plan from structured data together give all the rules that we need using the graph construction plan and the graph extraction plan. This red box down in the corner then is just a single tool that has multiple tools internally. The actually can take those plans and do the extraction itself and do the construction itself. It'll loop over all the construction rules to create the main graph, loop over the markdown files, chunk them up, do the vector embedding of those, and then also extract entities and the facts, and then connect those to the structured data. In lesson four through eight, you'll go through the entire workflow for structured data from user intent, file suggestions to the schema proposal. Then we'll skip over to the unstructured data, just the entity and fact type proposal, and then finally we'll look at the particular tool in lesson eight that does the graph construction itself, does all the hard work that the previous agents really did the reasoning about what the work should be. In the next lesson, we'll actually get to doing some coding and building your first agent. We're going to be using Google's A D K. If you're already familiar with Google's A D K, you could skip over that lesson and move on to actually building the multi-agent system. But I think it's a really good overview and also helps you understand how I think about it and how I've implemented it in this particular multi-agent system. So it's nice to walk through. I'll see you there. To build the multi-agent system, you'll use Google's A D K. Or And then a bunch of stuff from Google ADK itself. We're going to import this agent, which is going to be the key thing. Actually, just writing what the agent does for interacting with an LLM. We're going to be using the Light LLM library. So we're going to import that here. And this is a little wrapper for letting Google ADK talk to OpenAI. And then we're also going to need some memory for the agent. For the memory, we're going to use memory session service. And then finally, the agent also needs a way to run, so we're going to import a runner. We also have some typing stuff here that's not really so important for how all this works. Let's go ahead and make sure that all that imports, great, all the libraries imported. So the next part then is to start defining the models that we're going to use. We're going to be using OpenAI for this course, so let's go ahead and import. We'll define what the model is going to be itself is OpenAI's GPT-4o. Again, we're going to be using Light LLM for actually reaching out to the OpenAI API. And we're just going to go and try a sample execution of completing a chat with that. So if you look at the chat here, you'll notice that we're just sending a message that we're going to send to OpenAI, and we're going to ask it the question, "Are you ready?

". So if that all works successfully, we'll get a good message from OpenAI letting us know that this works. Looking at the model response, there's a lot of information here, but the key part is that the response includes a message from OpenAI saying, "Yes, I'm ready. How can I assist you today?

". OpenAI is all set to go. We're also using NewFJ today, so we're going to import a helper library that combines NewFJ with Google ADK in a nice way. And that library is something we can take a look at, see the details of what's going on. The import that we're going to use is just from NewFJ for ADK, import the GraphDB library. Let's take a look at those details. Okay, now we're inside of NewFJ for ADK, and you can see it starts off very simply. We import some stuff from the operating system. We of course load up the environment, and then from NewFJ, the Python package, we load up the Graph database, and you also get a result class definition. Now a bunch of these functions that are here are just really nice for just wrapping up how to interact with NewFJ and presenting the results from NewFJ in a way that Google ADK likes to see things. One of the key things is that results should come back either as a dictionary with a status that is success or status that is error. These two helper functions help do that. You can call to success or to error along with some extra arguments, and actually get a nice formatted result from any kind of call. To that, we then go ahead and also integrate a helper library. Results that are handled by Google ADK need to be easily persisted. So this function call here to Python basically goes through all the different kinds of results that can get back from NewFJ and makes them easily serializable and a nice format for Google ADK. You can look at the details if you want to see what's going on here. The key part, of course, is this function that takes the NewFJ result, you're passing in as just a result. Basically, calls that to Python package. Package is all that into a result that is easily used by Google ADK. Each of those helper functions is then also used by this upper level class called NewFJ for ADK. And this is really a wrapper that combines a NewFJ driver, initializes the environment variables used there, allows access to the driver, then has a special function here for sending queries, and it'll send any kind of query back to, but then uses those helper functions actually formatting those results into a nice Google ADK result. At the end, then, because we're just going to have one driver and one connection that we're using, we're going to have a single instance of this class that we use. This is the GraphDB that we're going to actually expose as a variable and use that through all the notebooks. Okay, we take a look at NewFJ for ADK. Let's continue with the notebook. So now we've got everything set up. We can start to define the agent itself. Okay, with our NewFJ wrapper ready, let's go and give it a try. You can just send any kind of NewFJ query that you like to, as long as it's valid. Here, the query we're sending is Define this function. Here, the function is just say hello. It takes a single argument, person name, as a string, and returns a dictionary. Of course, that dictionary is Google A. D. K. Dictionary that likes to see all results in. And it also has a couple important parts here. There's a doc string that's here, which is always nice when you're writing code. It's also especially critical when you're developing tools for Google A. D. K. This doc string is what lets Google A. D. K. Understand what actually this tool does. This gets passed along to the LLM. When the LLM is told here's all the tools that are available, and also here's what those tools are described as doing. So, this a couple important parts we should go through here. You describe what the tool does. Here, it just formats a welcome message to a named person. Of course, passed in is person name. You describe the arguments that are being passed in, and also the result. The result here is consistent with all the tools, because the result is a dictionary that Google A. D. K. Likes to see. But you can add a little extra detail if you want to here. For instance, if you know the kinds of errors that might come back, you explain that here as well. Inside the function is incredibly simple. All we're doing is using Neuraj again to actually do hello world. We're going to call it to Neuraj, send a query where we're still going to return a hello to you. But then we're also going to concatenate that with this person name, which is passed in as an argument to the function. That's going to get passed in as a query parameter to this query. Whenever you see that dollar sign in a Neuraj Cypher statement, that means here's a query parameter. So here in this array in this dictionary that's being passed in, you'll see the person name is being passed in as person name. That gets substituted in as this variable here, not as a template substitution, but as the value gets passed in. Then this whole query will get executed. So let's define that function. Of course, because this function is just a normal function, we can try to run it. So we can print saying hello to A. B. K. And expect to see a very nice Google A. D. K. Formatted result that was successful. Where the query result that was sent off to Neuraj has the reply hello to you A. B. K. Doing that concatenation. Fantastic. Now just to side note here about query parameters, part of why we're doing that this is good discipline for any kind of query language. They're using most query language we just have the ability to have query parameters. And one of the reasons you do that is to avoid injection attacks, right?

That people can't pass in values that look like SQL statements or Cypher statements or any other query language statement as a value and have that concatenated into a string that can cause all kinds of mayhem. So instead, you pass in a query parameter. And here, for instance, if I pass in the name, say actually the name is going to be hey, return some string here. This could be some malicious code instead. Get that gets passed in as the person's name. If you just concatenate that as a string, all kinds of bad things can happen. Instead, because it's a query parameter, that still just behaves as a variable. It gets passed in, it gets concatenated as a string with the other string, and the result is simply hello to the somewhat friendly malicious injection attack, and of course, it's been avoided now. We have the basic tool available. Now we can define an agent that's going to use that tool. Now there are a couple components of defining an agent. It's common across all frameworks. They'll do some variations of the same idea. Let's take a look at what's happening inside Google A. D. K. To define an agent. Now, recall earlier we had imported this agent class from Google A. D. K. So we're going to make an instance of that class and take a bunch of parameters. So the first thing that's important is that every agent is given a name. Here we're going to call it hello agent underscore v1. This is in case you have multiple versions of the agent that you want to have at the same time, or if you want to have different version instructions as well. Having an idea of what the version of this agent is is very helpful for debugging later on. You also pass in the model that you want to use. Where if you call, we define LLM, the four is being a call through Lite LLM out of AI. So we're going to use that here. And these next two are pretty critical for how the Google A. D. K. Not just from the tool definition, but also from your instructions you're giving it how to think about that tool and when to use that tool. So here, if the user provides their name, you know, use the say hello tool to give them custom greeting. And the final part, of course, is we have to make those tools available. The tools get passed in as an array of just the function names themselves. So we're going to pass in say hello, and then the agent has access to that tool. It knows what to do with that tool because we give it instructions. And other agents, if there are other agents, know what this agent's role is. Now that we have an agent, and the agent has a tool and has instructions about how to use that tool. Now we need to run the agent. This is the final part in creating a very basic agent system. An agent has to have an execution environment. So if you look at this diagram here, you'll see that inner box here. This is the execution environment that happens. There's a runner class that actually manages all the event loops basically for calling out to the LM, passing results from the LM to particular agents, and coordinating all the agents themselves and how they execute. Each of those runners also has access to various services, whether that is in-memory services for like action memory, memory could be in-memory or could be you could use databases like for data for memory as well. They get to access to storage. Then there's the execution logic that inside the execution loop. Here are the steps for actually running things and coordinating the agent, whether agents are running in parallel or in sequence or in a loop. This all together actually is the execution environment for the agent. Okay, we're going to do this by hand first, and then show how we can wrap this up to a little convenience method. When you're doing your own development with Google A. D. K. , Google A. D. K. Has really great tooling for just defining an agent and using the tooling for actually running that agent, providing it the full execution environment that it needs. We're going to be doing that by hand inside of the notebook, so let's do that one step at a time, and then wrap that up into a little helper. So there are a couple things you're going to need to actually set up the execution environment. Every runtime actually runs within a session. You of course have multiple sessions that are running in some production environment. Here we're going to have a single session. We're going to go ahead and set up the in-memory service which provides context and state for the agent as it's running. We're going to create the session service based on that memory, and also for the particular agent that we're writing, and also assuming that we have a user, we're going to assume that we have a single user, a single application running, and a single session. All is going to be used here in the create session to actually create a single executable state to actually be run, and then the runner is going to run that state, given the agent that it has. So here, the runner combines the agent that we want to run, our hello world agent. The app name is going to be the same as we passed into the session here, and also the session service is going to be the session within which the agent runs. Again, in a production environment, there might be multiple sessions, there might be multiple applications that are using this agent. That's why these are all different arguments here. Okay, now there's a lot to walk through just to actually do the running of the agents as well. So let's go through this step by step and take our time. We know that we just have this runner available that we just defined. We're going to do just a single loop through with a single user message and allow the runner to execute the agent reacting to that message from the user. So we're going to define a user message, and I'm simply going to say hello, I'm A. D. K. , and we'll go ahead and print that message out in friendly ways so we ourselves can see as this is running what's going on. That message that is just a string has to get packed up into the structure that Google A. D. K. , the runtime environment expects. Usually, this is handled behind the scenes for your course, but we're going to do this by hand. So we're going to create this content class here, and the role of the content that we're creating is for the user. This is the user message. Content can have multiple parts, so we're going to create an array of parts. Where one of the parts is has some text, where the text is a user message. This is all bundled up to do more complex things. Here we're just trying to run a single message from a single user, but this gives you lots of flexibility when you have That any verbose output right now, and you can see in a second what the verbose output might look like. This is the event loop itself. Well, one step I should say the event loop. You know, if for this one message, we're giving the user message, we're going to asynchronous use the runner and call out to the LM using the message that was passed in the agent that was defined to use that model and all the instructions we're giving it. So the first thing we'll notice is that when we call the async thing, we also pass in all the same information we had for creating the session. This is because you can run things again; there can be multiple sessions going on. You have multiple event loops running around, and asynchronously, you pass in here's the context currently operating in. It's for this user and this session, and here's the content that's being passed in. Go ahead and execute this one with the LM doing some work on this. This is inside the for loop because this is an agent system. So once the agent is given this message, it might do a bunch of things before actually having that final response. So the event loop here pulls that event from the asynchronous running of the agent. Every event is some update from the agent itself. As the events come in, if verbose mode is turned on, we'll print out here's an event that happened. Here's who created that event. The initial event of course should be that the user message was passed in, so we should see the event author was the user themselves. And you'll see the content of the event as well. You can take a look what's happening here in the debug statement. A bunch of things get printed out, which are helpful to understand what's going on. And then this is the key concept for keeping the event loop running for as long as you'd like it to run. Hopefully, the agent will run only as much as it needs to. But as each event comes in, there's a flag that says okay, this is the final event. I'm done processing this. The agent is done processing. If it sets this, then you know that okay, the processing is over. You can take a look at the content to see what the final response might be. Here we're taking a look at the content. If they have some multiple parts, so if it's a non-zero array of text that comes back, we're going to take the first message back from the part zero here, grab the text from that, and here we're making some assumptions that the text response in the first part is actually the right one. Turn that into the final response text. Alternatively, the agent could have decided this is my final response, and by the way, I don't have actually a response, but I've asked the runtime system to do actually escalate. Now here escalation means meaning the agent is running knows that it's a sub agent and it can't handle or can't do any more work with what's currently available. So it's basically saying hey escalate this to somebody else. Either my parent or to some other agent that might be appropriate. In that case, the final response text that we're going to set is based on that. We're going to say that the agent decided to escalate the current handling of the message, and we don't have a specific message or there's an error message. If there is one there, and return that as the final message. All that is continues to run through one round through the event loop, instigated by a single user message, and then at the end we'll go and print out the final agent response. That's kind of a lot to go through, but keep that in your heads a little bit. As you're going through all the books, internally this is really what's happening. Actually, make all the the machinery run for all that work. There we go. Sample chat. Hello, I'm MBK. Hello, you MBK. Of course, because we're going to be doing a lot of calling to agents manually, we're going to set up a helper class. Actually, let us do that a little bit more easily. And I'm going to call that helper class agent caller. This agent caller basically wraps up those functions we went through each one at a time here, and also the event loop itself into one class. And if you look through the details, it is doing all the things we've seen above. It's saving the single user ID, the single session ID, the current agent that we're going to be running, and also the runner that's been set up. It has access to getting the session that uses internally for actually managing stuff, and this call function here is what actually instigates one round trip through the event loop based on a user message. So it does just what we saw above. Takes the user message, packages it up so that it's available as content to be processed by So the agent caller, you will be passing into it all the necessary information that it has. What's the agent that you want to be running, and also what is the initial state of that agent, where the state is the agent's internal memory. All of this should look just like what we've run through before, but now packaged up into here's the different elements, the major components that should be combined into the agent caller. What is the current app, the user, the session ID, create a session service for both memory and also for managing the session itself. Then finally, also create a runner. Then having constructed those things, pass those into the agent caller, which wraps those all up into an execution loop. Okay, now that we have some utility functions for running an agent, we can run a conversation with the say hello agent that we said that we created earlier. So to create a new agent caller for our hello agent, we're gonna actually call the helper function to make agent caller passing in the hello agent itself, and we'll just call that the hello agent caller. Then to run a conversation, we're gonna have an asynchronous function that has multiple user messages that we're going to pass in to the agent caller using the call method. First, I'll say hello, I'm ABK, and then I'll say I'm excited. We'll go ahead and run that conversation and see what that looks like. Awesome, we have created a friendly chat agent. Good work. So now that you've gotten this far, you know the basics about using Google ADK, how to create an agent, how to give that agent tools that it can use, then also how to create an execution environment for that agent. Finally, with all that in hand, you can script interactions with the agent here in the notebook, passing user messages that let you then test out how the agent is going to work. So this fantastic agent caller class we created, and then this construction method, the factory method called make agent caller, we're gonna use in that through all the other notebooks to make it easy for once we define an agent, we can easily create a runtime environment using that function. So you'll get a lot of use of that. In the previous lesson, you learned how to create an agent using Google ADK, and now we're gonna create multiple agents, a team of agents working together. We're gonna have a root agent and then two sub agents. We'll build on what we did previously, where we had the say hello agent. We'll introduce a new agent that can say goodbye, and then we'll have another agent that puts them together into one multi-agent system. As usual, we'll go ahead and start with importing the libraries that we need, and then of course we'll go ahead and set up the LLM that we're gonna use. Again, this is OpenAI. We'll define the LLM, and we'll also go ahead and do a quick sanity check by passing out a message to make sure that it's ready. Always fun to see what OpenAI might say and give them usually with friendly messages. It's pretty good about sending fun things back. Next, we can set up the agent caller. We're gonna use the same class we defined before in lesson three part one, rather than defining all that again. We're just gonna import the tool that we need. As you remember from part one, we had the helper function make agent caller that's gonna create the agent caller class with all that it needs to run. Okay, with everything ready, you can now begin to create the multi-agent system. You're gonna create multiple specialized agents, each designed for a specific capability. You'll create one agent in charge of handling greetings, that's the say hello agent we had before. The new agent is gonna take charge of saying goodbye. To combine those, you're gonna have what's called a root agent, and you'll hear terms like orchestrator or coordinator, top level agent. It'll really mean having an agent that's gonna wrap up two other agents and then manage the execution. That root agent is gonna receive the initial user request, the hello or whatever the user decides to say, and then decide how to process that either itself or by what's called delegating, handing off to one of the sub agents. So the root agent, the top level orchestrator, is gonna be delegating both hello and goodbye, hopefully, to the sub agents that specialize in those tasks. Okay, let's start to assemble our multi-agent system. Again, we're gonna have the say hello agent that we had before, we're gonna add a say goodbye agent, and then we'll combine them together. First, we'll define the tools for our sub agents. The first tool is the same say hello tool that we had from the previous lesson. This is the hello world function, takes a person's name and creates a hello world message as a response. To that, we're gonna Now, as we're defining these subagents, I really want to emphasize the best practices here and how important it is to have really good descriptions of each of these agents and also instructions you're handing to them. You're going to spend most of your time once you set up any kind of multi-agent system. Most of your time is going to be spent actually optimizing the instructions. This is back to classic prompt engineering, and also this description. To repeat what I said in the previous lesson, the description is what allows other agents to know what does this agent do and when should I use this agent. That's called delegation. And the instructions are for the agent to self-understand what is its purpose, what is it trying to do, and what tools does it have available and when to use them. So this is our greeting subagent. It's the say hello agent that has access only to the say hello tool. And then we'll add our farewell agent. The farewell agent is pretty straightforward. It's similar to the hello agent. You can look through the description here of what the instructions are, and these instructions are actually useful to take a closer look at. It seems obvious. What's your job as the farewell agent?

Is to respond whenever the user says goodbye in some kind of way. Well, people say goodbye lots of different ways. So this is a very small example of how to do some fun working within the agent instructions. So here we have in red. This is some examples like when the user uses words like bye, goodbye, thanks, bye, or see you. Those are all ways of saying goodbye. Now, most AI models don't need this extra bit of examples, but this is a good practice to have overall to think how can I help the AI understand what this agent's purpose is, actually when to execute and what to do in its role. And of course, we're going to hand it the say goodbye tool that we defined earlier. Great. You now have two subagents ready to go. Now we need to combine them. We're going to do that by defining the root agent, which is going to understand both of these subagents and when to delegate a task to them. It's similar to doing tool calling, but the agent knows that it's talking to another agent, so the entire conversation history gets passed along, and actually control of the conversation gets passed along to the subagent. So it's a little bit different than tool call. The same idea is current workflow is going to go from the agent who is in charge here, the root agent, to one of the subagents. So this is similar to tool calling, but let's take a careful look at how agent delegation works here. We know that the agents themselves described what their role is, like the the say hello agent versus the say goodbye agent. Both have some understanding about what it is they're supposed to do, and their descriptions describe to other agents what it is their job is. We're going to double down on that instructions we give to the root agent. The root agent should be told your job is to coordinate a team of subagents. So you can see that in the instructions here, and that its primary goal is to be friendly. In order to be friendly, it has actually two special subagents. So we describe what those are so that the agent understands when it actually should use the subagents. This is like describing when you should use particular tools. The greeting agent should be used for handling simple greetings like hello or hi, and of course the farewell agent should be used for saying goodbye, whether that's bye, see you, or anything else. And we're explicit here about saying when those messages are received from the user, we're going to delegate these subagents actually respond to the user rather than responding directly as the coordinator. So if this works well, the coordinator shouldn't do anything other than answer general questions and when you say hello or goodbye, it should delegate to these subagents for actually executing or responding or generating response to the user. This top-level coordinator doesn't have any tools to use whatsoever. It can only chat or delegate to the subagents. We're also going to hand it the list of subagents here in the subagents key. So the subagents are the greeting subagent and the farewell subagent. Now that we have a team of agents all put together into a multi-agent system, that's ready to interact with it. This is similar to what we did in part one of lesson three, where we had an agent coordination that managed the conversation. So here the conversation can be just what we did before, hello and A B K, and then later rather than saying I'm excited, this transcript is going to say thanks bye. This can be the second user message. We've also added this trace here, so if you look at the call to this agent caller, the the second argument that's passed in is whether or not to be verbose in responses. We're The friendly agent team. So this is the top-level coordinator, so the team coordinator that's going to call the subagents. And you can see the first action it takes is that it wants to transfer to agents, so transfer or delegating to a subagent, and it's going to transfer to the greeting subagent. So that's perfect. In response to "Hello, I'm ABK," the top-level coordinator realizes I have got an agent who's really good at saying hello. Let me transfer control over to that agent. So the transfer response doesn't have any, you know, value whatsoever. It's just a none. What we should see next is that the greeting subagent takes control. So here's another event, but now from the greeting subagent, and it's going to look at the transcript and see the user's message and realize that, okay, it's now my job to actually do something about this. The greeting subagent is going to respond by making a tool call. You can see this function call here, and the name of the function that it wants to call is say hello, and it's going to pass some arguments. Where the one argument that's passing in is the person name is ABK. So the agent is realized that from the statement here, of course, "Hello, I'm ABK," is able to extract that ABK must be the person's name, and it's going to make a function call to the tool passing in that name. And then you can see the response. Here's the function response, and the response is "Hello to ABK." Perfect. Now the greeting subagent is done. It goes ahead, and you can see that the final message is through. So that's the flag that says I'm done processing this message. We can then terminate this particular event loop, and the final agent response is being the response came back from the tool "Hello to ABK." We send in the second query then or message from the user, and the user says "Thanks, bye," and again the top-level agent is going to go ahead. Well, actually, no, it's still in the control of the greeting subagent. So the greeting subagent itself is going to realize, okay, this is not something I should do. Please transfer this to the farewell subagent. So it's aware of the other agents that are available, transfer to the farewell subagent, and that is the end of that function response. Here you can see the message from the farewell subagent itself. Again, it's going to look at the transcript. Now that it's been given control of the current conversation, the first thing it's going to do is realize, okay, the user said goodbye. Let me go ahead and call the say goodbye tool to figure out how to respond to them. And so here's the function call to say goodbye, and of course it takes no arguments, as expected. But the response to it is the constant that we saw earlier in the tool definition. It's going to get a farewell that is goodbye from Cipher, and that again is the final response for this event loop, and so that is going to be the final agent response, goodbye from Cipher. Okay, that was a lot to walk through. It's worth taking the time to do that for anything else we do in these notebooks. You can really see all of the interactions about tool calls, agent delegation, and also the responses and the changes in state overall. So really useful to do. We're not going to do that for every notebook, but if you'd like to on your own time, please do take the time to really understand what's happening behind the scenes, because what's happening behind the scenes is going to affect, of course, how you do the overall agent definition and also the agent registration. You're going to take one more step in the multi-agent system. We have an execution environment for agents. We've got a team of multiple agents that can work together. They can delegate tasks to each other, and then each of those agents also has tools that they can use. The last important component for all agent systems is having memory. Memory is just internal state happening for a particular session or across any agents that are involved in that session, and of course the user themselves. What is session state?

Really, session state in Google ADK by default is really just a dictionary that's available that you can update keys within that dictionary, and when you update the values of those keys, Google ADK tracks those changes, it tracks deltas to the state basically, and updates the overall session state to keep consistent across all agents, whether they're running in parallel or running sequentially, because it's a synchronous system. It's hard. It manages all the state updates. Agents have a few different ways of interacting with state. One way, the way that I prefer, the primary method of interacting with state, is through the tool context. So whenever a tool is Memory for access and memory. You can take the actual output from the agent, that final response, rather than just turning that back as the response from the agent. You can save that in the state by defining an output key. So when you see this final trail, the message related to the final trail here to be the text goodbye from cipher could get used for updating state itself. That's very convenient sometimes, and we'll do a little bit of that later on in some of these notebooks. Okay, in this next step, we're going to set up some memory. We always have the in-memory session that we're using right now, so the memory is saved just within RAM. It's not being persisted to a database. Of course, that's the best thing to do for a production system, but for convenience, having in-memory state is perfectly fine. We're going to update both of our tools to actually take advantage of using that in-memory state. So both for say hello and say goodbye, we're going to update those tools to take advantage of the tool context, update session state or memory. You should do this one at a time and take a careful look at the differences here. You'll notice that we're importing tool context from Google A. I. Tool context is the class that takes care of keeping track of the session state and an additional other thing that is useful for tools to have access to. So we're going to update the say hello function to be say hello stateful. It still accepts an argument for the user's name, but it now has this extra argument called tool context. It's the tool context that's going to let us get access to the tool context passed in for the session by the execution environment. So in the tool context, there's this state dictionary, and we're just going to update the user name key and the name that was passed into the function. We're going to use for updating the session memory or the session state for the user name key. So this is pretty straightforward. User name, user name. Now we have a dictionary that is shared across all agents and across all tools that will have that value that's passed into the function. This is a way of sort of keeping track of things by natural use of functions, and also then preserving them for later use. For debugging purposes, when this tool is called, we'll get it printed out the update to the session state, particular to the user name key, and then of course we'll do what we had done in the previous say hello. We'll send a query off to N. U. R. J. Using a query parameter that is the user name. So let's go ahead and define that. Now that you've defined that, you can define the same thing for the say goodbye. And again, as you're defining this, this is just an update to the say goodbye that we had before. Now the difference is that this say goodbye, even though it doesn't take an argument for anything related to the person, has no custom arguments here. It does get this tool context passed in. And I should mention that this is something that Google A. I. Does automatically when it sees a tool, where the last argument to the tool is tool context, it automatically injects that to the tool call. So when say goodbye stateful is called here, it's going to get tool context, but nothing else. But because say hello stateful has set the state, the user name into the state, say goodbye can actually access the user name. So now we've remembered what the user's name is. We can access the state for the state key user name, and then have a default value in case the user didn't identify themselves. But we can get it from the dictionary the user name or default assign that to the user name, then use that for actually generating a goodbye statement. You can then define a new stateful agent for farewell, and again that's going to be the same as we had as the previous farewell agent, but now it's going to call the say goodbye stateful tool as before. You can combine those into a multi-agent system by having a root level agent, a coordinator agent that's going to use both of those as sub agents, and this is exactly the same as the other root agent that we had, but now it's just using all the sub agents that have ultimately down at their core these tools that are stateful based. You now have a multi-agent system that takes advantage of memory. You can give it a try. You will use our friend the make agent caller, passing in that new root agent stateful that is the top level coordinator, create a caller for that, and to see the change in the session, we'll go ahead and get the session directly from the caller. Remember there was a utility function for getting that if you look back at the agent caller class. We'll go ahead and get the session, and then based on that session, we'll Actually, I'm going to take out these verbose statements right now, and you can decide to keep those in or add them if you'd like. Run it either way, but just keep the output to a minimum. We're going to run this without the debugging statements, and what's really important here is that we saw that the initial state was empty, and we expect that once these calls are done, the initial state should turn into a final state. Here we're going to get it again. That the final state should actually have the username as defined by the call to say hello. Awesome. So our utility function is printed out. This updates the username here, and we also see when we get the session and then get the state from that session that the final state includes the username ABK. Fantastic. Everything's working as expected. Now, if you'd like to, you can actually run this in the interactive environment, even within the notebook. You can set up a small helper function that's going to have a loop for getting messages from the user, then just calling our caller passing in that message. This will run as long as the user doesn't pass in a message that says exit. When you set that up and then run it, you'll see a little box appear. So when you run the interactive session, you can type in a message and just see what happens. I'll start by not revealing my name. See what happens. Okay, that's kind of funny. So the LM decided that my name is there based on this message. That's not really great. Let's correct it. Let's say, and you can see just by passing in my name is ABK that the tool context actually updated with ABK. So there's a tool call still to say hello ABK, and because of that, the LM correctly identified my name is ABK, saved that into memory. This is fantastic. If I say goodbye again, we should now get a nice message by ABK as before. Give this a try. Interact with it and see what you can come up with. And of course, if you'd like to change the end of the code and see what the impact is of that. Okay, you've gotten all the way to the end of lesson three. You've created a basic agent that is a single agent that says hello, and then you created a multi-agent system with two sub-agents specialized in saying hello or saying goodbye, and then a root agent that coordinates their interactions with the user. With this in hand, you're ready to continue to actually doing agentic knowledge graph construction. In this course, you learned how to build a multi-agent system that transforms your structured and unstructured data into a knowledge graph. You learned how to set up each specialized agent in your system, what prompt to use and what tools to define, and how to share context among the agents. Congratulations on completing this course, and I'm looking forward to seeing what you'll build on your own.
