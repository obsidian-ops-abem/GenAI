---
title: "81_LLM数学基礎 トークン化からTransformerまで（出典）"
tags: [raw-source]
source: 学術講義（LLMの数学的基礎・確率モデル・Transformer）
author: 講師名不詳（ASRから特定不能・大学教授・確率論・最適化の専門家）
published: 2026-08-04
created: 2026-08-04
---

# 出典メタデータ

- ソース: 学術講義（LLMの数学的基礎）。文脈から機械学習コースの1回（"Chris"という別講師が前回を担当）
- 講師: 不詳（大学教授・確率論・最適化理論の専門家。"one of my papers"でHessian/局所最適の理論を言及）
- 公開/取得: 2026年8月4日
- 形式: 文字起こし（`20260804_131940_transcription.txt`・ユーザー提供・WSL上で生成・169行）
- 内容: GPT系自己回帰言語モデルの数学的基礎。トークン化(BPE)→条件付き確率の連鎖律分解→埋め込み→softmax→生成(温度/top-k)→負の対数尤度訓練→Transformer(attention/マスキング/マルチヘッド)→MLP→ReLU活性化→残差接続→LayerNorm/RMSNorm→SGD/minibatch
- ※自動文字起こし。話者ラベルなし・数式の黑板記述が文字化け・"Q and Q and three point five"(Qwen3.5)/"flexension"(FlashAttention)/"RMSE norm"(RMSNorm)等の誤変換が想定される

---

# 文字起こし（全文）

So language models, I guess you always probably all of you have heard of this word, language models. This is um um precise. I think in some sense, this is all starting from GPT3 when open released GPT3. Um, it has um it uses new paradigm which is auto regressive language models. Um, so um so basically we focus on that, you know, auto regressive language models will skip some of the earlier versions of transformer which are um not auto regressive. Um, so and the and auto regressive basically means you generate tokens one by one, and each of the new tokens depends on the previous tokens, and you do this um and you model the probability in this kind of like a auto regressive way. I'm gonna talk more about details. In some sense, you model your language distribution by using a um um a sequential sequential model one by one in some sense. Um, anyway, so um let's um I think the point our goal is to talk about some details. So I'll feel in more high level stuff as we talk about details. So the first thing we're gonna talk about is tokenization. This is um you know just um to prepare us to talk about the real meaning in some sense. So what does tokenization mean?
Means that you need to you know this transformer, this new neural you know architecture models, right?
So they take in in some sense new neural numbers, right?
So um but you have text, and how do you turn text into some kind of things that the models can understand?
So the first step is to tokenize. Tokenize means that you need to decide what is the smallest unit of the input you give to the language model, and often is not a a word, right?
The most natural thing you can think of is maybe the most thing is is the character or word. If use character level tokenization, then the issue is that often you have so many characters, right?
You need so many tokens, so many steps. Um and if use word level tokenization, then often the issue is that you don't how do you deal with this long word, which are very rare. For example, you have a word called say internationalization, right?
So if you treat this word as a single word, then maybe it's too rare, and you don't leverage understanding about other words. So if you treat internationalization as one word, then it is different from internationalized. It is different from like a nation, right?
And and they are all completely different concepts, and you cannot leverage um that you know for example if you understand internationalized, then you should probably understand internationalization to some degree without even seeing more data. And so um so that's why people don't always use um sub word tokenization, no, don't use word tokenization. Maybe another okay, maybe I make another example. Right, for example, if you have use this, I guess in my notes there is this word I am, infication. I actually don't know whether this is a real word, you know. I think it's a new word that is completely new word. Nobody have used it before 2020, 2020, I guess, right?
So even even right now is not that kind of like common. And if you have to treat this word as a unit, then you you have to see this word enough times to understand what it means, right?
However, if you treat as sub words, then maybe you have a a token called I am, you have a token which is you have a two uniting. This is a token, this is a token. So I am is a token, infication is a token. Then in some sense you have seen I am many many times, you understand it, and then infication has a special meaning in English, and then you combine this two understanding, you you understand the word. So that's much more efficient understanding of the word, and then just the treating this whole thing as a single unit. So um and then and this happens more often in, for example, biology where you have this very long word, or maybe in German you have very long word, but actually they are fundamentally many combinations, many sub words, and the and the and using sub word tokenization allows you to understand each of the sub words, and and then you can understand the combination of the those sub words as as as a whole very efficiently. So yeah, so basically tokenization, you know, just means that you have a predefined vocabulary vocabulary, and this vocabulary is a list of like sub words, right?
So for example, you can have you know many many of this, like maybe a is a sub word, you know, is a one of the token, you know, um I think maybe like dog is a token. Pretty much all common words these days is a token. And actually, if you look at the the actually you can play I think for open source models, we know the number of tokens. And they have, I think, is you know, often on order like ten to the five, like a higher K IDs. I think Q and Q and three point five. This is the newest version of open source models, which are pretty good. And they have like two hundred fifty tokens. I think two hundred fifty probably mostly because you know, I think two hundred fifty is somehow you can represent token with like maybe eight bytes in some sense. Like like it also depends on how many bytes you can present the tokens. So anyway, so you have this vocabulary, and then you just give me a long string of text. You just take all the subwords that are tokens, and you turn it into a sequence of like segments. Right?
For example, if you say, if you have a dog runs, suppose this text you see, and then you you say this is a token, so this is one part, and then I think blank dog is a token, and blank runs is also token, and and comma I think is a token itself. So and every token has an ID, right?
So let's say A is probably an ID number one, dog is maybe an ID number like two hundred fifty, say whatever, and and one is some ID, maybe I don't know, like maybe two hundred fifty. I'm just writing some random numbers. Every every thing has an ID. So basically, you turn every text into a sequence of IDs. You know, each ID corresponds to one token. And and I think so. In this case, you know, most of these words are just one token. I think I just check recently. If you have the word happiness, I think the tokenization will be happiness. Let's say I think this part is a token, H is a token, and A P and E S is a token for some reason. And maybe another example is this. Oh, I already read it. I am investigation. And this is another example that I come we come up with. You know, just I don't know actually what this word means. If you have a plus comma, I think this part is a token. This part is a token, and this part is token. I think this is just because for these biological biological words, you know, biological words, you know, every part of the word has some special meaning, and so you tokenize them into tokens. And and what is the exact vocabulary?
It depends on model. So basically, the algorithm that turns the text into these tokens is called a tokenizer. And and every model has a different tokenizer. Not not everyone has a different one. Some some of them share the same tokenizers, but generally, it's specific to the tokenizer to to the model. And different companies have different proprietary tokenization approach. You probably heard of that. Recently, I think Cloud Code, Cloud they they their tokenizers so that I think they make the tokenizers more granular to some degree, so that the same amount of text will be tokenized into more tokens. So before it's like one thousand tokens, now it's one point five thousand tokens. You know, I didn't verify myself by that reason. Some models, assuming they are true, then means that you you spend more tokens. You know, you pay more to Cloud Code with the same amount of text. Anyway, so cool. And there are some details about how to come up with the vocabulary and how to kind of like when this algorithm tokenizes. The the keyword is BPE BPE tokenizer byte pair encoding. So I'm not going into the details because this is just a very small quick setup. It's not that important. But roughly speaking, the BPE tokenization idea is that you try to in some sense just try to find frequent subwords that are most frequent, right?
And and use them them in the vocabulary. But how do you decide which one is more frequent?
And now I think there's a sequential algorithm. You first go to maybe like the the the smallest kind of like combinations of like you go through all characters, you know, and you you try to find you know, I think it's a greedy algorithm. First, you try to find the most frequent, you know, one character thing, and you find the most frequent combination of two characters, and you remove. You know, it's it's a little complicated. Which I I mean, I'll try to give you a precise version, but it's kind of like a greedy algorithm that decides which one which is more frequent. So and so forth. And if you are interested, you can look up the exact algorithm. Okay, so that's the tokenization. So that means that from now on, you know, we just assume we have a tokenizer. So every text, every piece of text, will be turned into a sequence of tokens. So from now on, like a text just This is kind of like index in the vocabulary, like this is the fifth token, this is the hundred fifth token, so on so forth. Okay, so now the question is, you know, now we go into the next question, which is how do we model this distribution of sequence of tokens?
How do we create a probabilistic model to model it?
If you think about the power of this this distribution, like how many possible combinations that are possible here, so the number of possible combinations is you have V choices, this is a kind of like V, you have V choices, kind of like V choices for each of the the tokens, and you have a sequence of length T, so you take the power to the T. So every one has V choices, and V to the power T is the total number of choices. So you cannot assign one probability for each of this sequence, like like like like like like discrete categorical tokens, right?
Like categorical distributions, right?
If you treat this as a categorical distribution, then every choice here will have one probability, and that's just way too many parameters to describe. So that's why what people do is that people decompose this distribution using the chain rule to write it this way. So you say suppose I have a probability distribution over the joint of the sequence, then I can write it as the following. So I can say it's equal to P of X one times P of X two given X one, P of X three given X two and X one, so on so forth, and P of X T given X one up to X T minus one. And this is the the so-called you know conditional probability decomposition. You decompose the joint distribution into a product of conditional probabilities, and you model each of these conditional probabilities. The benefit here is that each of these conditional probabilities is a distribution, is a conditional distribution, but the choice of X two for example here is only V. You have V choices here. You only have you only have to have kind of like a softmax or logis over V choices for each of this, right?
So each of this will be eventually see is some kind of like um um you have some logis and you apply some softmax to get a probability distribution over V choices. So that's what we're going to do. So basically, each of this will be parametrized by a neural network. The input will be what after the conditioning, right?
The input here is the X one, X two, and output is the distribution of X three, and this input-output relationship is modeled by a neural network, which is the transformer. Okay, so that's um what we are going towards. How do we model each of these conditional distribution?
So um so basically we're describing a conditional distribution P of P sub T of X T given X one up to X T minus one. So um before going into details, let's treat this as a kind of like black box first, and let's first kind of like discuss a little bit about you know the the boundary, right?
In some sense, right?
So it's kind of like multiple layers here. So the typical way to um um parametrize this is the following. So you use a transformer. So basically, what happens is that you have a sequence X one up to X T, and um so first of all, these are discrete IDs, right?
So each of this means a ID for the token. You first have to turn them into some numerical numbers. So the the first step is that you say I'm going to turn this into embedding E of X one, and so basically turn each of the token X into X T into embedding of X T, and what is the embedding?
Embedding is a let's say D dimensional vector in um the Euclidean space, and this embedding is trained. So basically, you don't know what the parameters are. You will just parametrize it, you know, as unknown numbers, and you train the model to find the right numbers um for for these embeddings. So every every um token in the vocabulary will be will have a corresponding embeddings. So basically um you have a um a sequence of embeddings E one, E two. This is embedding for the first token, embedding for the second token, up to the embedding for the last token. Um, and in this lecture, I think we're going to use row vectors because I think um we're getting to the implementation. You know, I will vectors actually row vectors in the in the Python code, even though in mathematics people often use column vectors. So um so I talk about this and and I thought just for this for language model section, we'll just always use row vectors, which is more consistent. With the practice, it looks a bit weird to me. Actually, from a mathematical perspective, I need to read the papers. Many of the papers use column vectors, but I think we just use row vectors to be consistent with the code. So basically, every embedding is a row vector, and then you have this embedding matrix. So this is you have v of them, and each of them is of dimension d. So you have this matrix, and and to read off which row you are, you just have to multiply. Either you just read off the id. So if you have the fifth token, you just read the fifth row. All you can multiply this matrix will indicate a vector to get that row, which is the same thing. Anyway, so we'll just read off from this embedding matrix the corresponding embeddings for each of the token. So you get e of x two up to e of x t, and and I think in many cases, often people append prepend a beginning sentence token, which is just a fixed token in the very beginning. This is kind of like not always necessary. In some cases, you just don't do it. In some cases, you do it. But let's probably just introduce that you have a beginning sentence token, which is one of the special token, and this vocabulary you probably often actually happens is that your vocabulary is bigger than what is needed. You always reserve a few tokens. You know, actually, maybe a thousand, you know, a few thousand tokens for special tokens that you can introduce in for special purposes, and you can introduce this beginning sentence token as x zero, which is always the same, and then you turn them into embeddings. And then what you do is that you go through this transformer, which I will discuss in the later half of the lecture. So this is a transformer. For now, you just think of this as a neural network, which does a lot of computation, matrix multiplication, you know, attentions, which I'm going to discuss a lot of operations, and then you output some loggers, like you output some kind of loggers for each of the locations. So each of these loggers is supposed to describe the conditional distribution before the the softmax. So basically, what you do is you say the probability of so let's just use say each of these u one is let's call this u one, you say x zero, and then p of x one is modeled as you know just the softmax distribution p of x i is the x one is modeled as the softmax of f theta x zero. So the first distribution, which is not conditional, you just get the output from the x zero. You get some loggers. So this u u i is in dimension v, and you take the softmax, you get a probability vector. So this is a probability vector, right, in this simplex of dimension v, right?
So the sum this this after taking the softmax, the sum of the entries is one, and all the all the entries are non-negative. So then this is your probability for x one. This is the distribution. Technically, I think the distribution of this is equal to softmax. Okay, and then the same thing. You know, we have x two, given x one, you say this is equal to softmax of u two, where u two is the output at the second position, and and u two, you know, we'll just call u two the same as you know, so we call u two the output, you know, of this model given x zero and x one. So so each of these u i's, you know, is basically the output of the sequence before it. So u t plus one is f theta x zero up to x t. So so so basically, generically, you know, p of x t given x one up to x t minus one is the softmax of u t, is it u t or u t plus one?
U t plus u t yes t, which is the softmax of f theta x zero to x t minus one. Okay, any questions so far?
So I I particularly choose it to index at one. Yes. Yes. U is just a vector, you know. I think that you know I can just call u. I think it's it's kind of like uh I can just call u as x zero. It's the same thing. I'm not did I answer the question?
So oh I think maybe maybe that's the so so I'm just using I say the x zero x one as more explicit way to represent u because I'm trying to express the dependency. So so basically I'm saying like uh basically I I think they are the same thing. Like just there there's no difference at all in some sense. Just how you view it, right?
In one way you just say oh I need a notation for the vector, and the other way is I have I need to interpret it as a function of the inputs. Okay okay okay so maybe I didn't answer the question. Yes yes yes so I think maybe I'm trying to figure out what's the question here. So um I think maybe what they're asking is the following. So so maybe let's take this one, right?
So this is a so this is a vector. This is a vector in dimension v. So in in the vocabulary size, and after taking softmax, it's a it's a it's sequence of numbers which are all non-negative and they sum to one. So and what this really means is that um p theta of x t is equal to one given x one up to x t minus one. The chance to see the first token under this probabilistic model parameter theta is equal to this is the vector the first entry of this vector, right?
It's equal to softmax of theta x zero up to x t minus one. Index one. This is the first entry of this vector, and you have many many other entries. Right?
Each entry is like you have another entry softmax of theta. Sorry. Two and that is equal is equal to p theta of x t is equal to two the chance of seeing the second uh second token given x one up to x t minus one. And you have so so so does that answer the question?
V is uh the yeah this v absolute value is the size of the vocabulary. Yeah yeah so this is the causal model so far. But I haven't defined the transformer. Yes. So so but I'm insisting that this is only so guys this is one of the reason I'm using this notation. So I'm insisting that u t is only a function of x zero and x t minus one, but not a function of the the the later inputs. So but that will be um that will be guaranteed when we introduce the exact inner working of this transformer. Okay good question. So the first the second question is your answer. So the embeddings is only a property of the this x right?
You don't have to see any other things, right?
It's just actually it's literally if this is the first token, then you just read the first row of this matrix. If this is the tenth token, you read the tenth row, and that's your embedding. No other dependencies. And while we are writing this as a as functions of x, but not embeddings, that's just uh it's just uh it's some of the embeddings is the inner working to some degree. I'm just trying to give you more. I'm trying I'm packing the right. So so so So if you don't have to imitate, you just work. You can work with the text. That's just the representation. Yeah. And it's one to one, so in some sense, there's no difference. Okay. So this is the full description of the probabilistic model. Once you have this probabilistic model, you can compute the maximum likelihood given any data. We can say I have a data distribution. I have a data point, a sequence of data. I can see, and this probabilistic model, please tell what is the chance of seeing this, um, this data. So that's that's what you need for computing the the training, right?
You you you need to compute the likelihood, and then you maximize the likelihood as a function of data, right?
So, um, and let's briefly discuss, you know, for generation, right?
So suppose you already have the generation or decoding. Suppose you already have the model. Then a model state. Then what do you do?
So um, as you can see, the most natural thing is you just keep generating x one, x two, x three, so on, so forth, right?
You just say I'm going to generate x one first from this, you know, um, p say softmax, softmax, you know, I have state x one up to x zero up to x t. X I guess that just right. Generically, so you generate x t minus one from, um, I have state x zero up to x t minus one, right?
In do this sequential. Sometimes we draw this in this way. You say, oh, you have x zero. You first go to the transformer, and you get x one, and you feed x one back to the transformer, right?
So, and um, and then you generate x two by sampling from this softmax, and then you feed it back. So that's kind of why it's called autoregressive, because you you have to when you are generating, you don't have the sequence, right?
When you are computing a log likelihood, you see the whole you already see the whole sequence. You just try to figure out what's the chance of seeing this sequence. But when you are generating, you start with nothing, and then you have to generate your sequence yourself, and you. That's why you start with the beginning of sentence sequence token, and you generate first token, and then you give the first token back to transformer and compute this softmax, and then you um generate second token, you do a sampling to generate second token, and you give it back, and you you keep doing this. So that's the autoregressive generation. Um, the um, um, and sometimes you know, like this generation only start from certain part because in many cases you you are not generating from nothing. So you are given maybe a few tokens, maybe you are given. So the sometimes it's called prompt. So prompt is like something the model is given, maybe like a question, you know, or instruction, so on, so forth. You say you are given x up to x k, and then the model is supposed to complete the generation. So it start with x t plus one, x t plus one. So this is given, and then you start with x t plus one. You first get x t plus one from this softmax of the I have state x one up to x k, and then you generate x t k plus two, and then you do the same thing. So it doesn't have to be always from the beginning, and then there's another small thing which is that when you generate, you don't necessarily have to always follow the same model. And there's one thing that you can do if from it, which is that you can um add additional temperature, where um you say as opposed to generating from the original probabilistic model, I'm generate I'm going to divide this by a temperature t tall or t sometimes. Um. So um, and the what this what what does this mean is that you change the, you don't change the orders between this choices, right?
So this is the, this this I have state is a vector, right?
So a V dimensional vector that describes the largest for each of the tokens in the vocabulary, and when you divide by tall, you don't change their relative orders. The bigger ones are still bigger, the smaller ones small, still smaller. But you change the the scale. So um, so for example, before you could have like the, so. Suppose you know I have state x zero up to x k, is is equals to let's say, maybe like two one, minus one, something like this, and if you divide by tall, let's say suppose you divide by tall, which is very very small. Suppose tall is almost zero, then after dividing by tall, like if divide by tall, then what happens is that maybe this becomes two The softmax of this is much sharper. So maybe let me just give an example here. So, so if you just have let's say maybe let's just have three dimension so that it's easier to demonstrate the point. So if you do softmax of this vector of two one minus one, what happens is that you get um sometimes you get exponential two over exponential two plus exponential one plus exponential minus one. That's the after softmax was the probability of the first token, right?
And the second token will be exponential one, exponential two plus exponential one plus exponential minus one, right?
And third one will be the the same denominator, a different numerator, right?
So that's the that's the softmax of this. But if you take the softmax of the two hundred one hundred minus one hundred, suppose to do that, then this will give you exponential two hundred versus exponential two hundred plus exponential one hundred plus exponential minus one hundred. Blah blah blah. So what's the difference here?
The difference is that in terms of the the three numbers, in terms of the order is always that this is bigger and this this is bigger and this, right?
Because two hundred is two is bigger than one hundred is bigger than minus one. The order is always maintained, but the the the relative um um scale is different because here you can see exponential two hundred is way way bigger than exponential one hundred. The difference is like so astronomical, like it's two the the difference is two makes like these two numbers are just so small compared to exponential two hundred, so they are even negligible. So basically in this calculation, these two numbers don't even have to show up here, like it it don't even matter. So you just get one. So basically the first token will just probably exactly almost exactly one point nine nine nine nine something, and then if you look at the second entry, which is exponential one hundred, divided by exponential two hundred plus exponential one hundred plus exponential minus one hundred. Again, everything is negligible compared to exponential two hundred, so this is not very useful. This is not very useful. This is kind of like close to zero. So basically this entry will be zero. So that's why after you you you divide by tall to make this bigger, then this is just approximately one zero and zero. So that's that's what I mean by much sharper. So you you you sharpen the the distribution to make it most of the distribution to be the the probability mass to be on the most likely token. And however here, exponential two and exponential one are not that different. If you compute this, you get something like maybe the first one will be like maybe point seven, the second one will be point three, I I don't know, like we can use a calculator to see. But it always still be like this one is bigger than this, and the second one is bigger than third one, but they won't be like all the mass is on the first token. So so so basically what I'm trying to say is that if tall is the small number, so that you you you amplify the largest, then you are sharpening the resulting distribution to make it more like a deterministic distribution, and then as a result you are just generating um you are just sampling from the the most likely token. You are taking the max as opposed to sampling from it. So and and and that's actually what people actually in um um do like they say basically like I think actually in APL you have to choose to choose the temperature, and if you choose a small temperature it means that you want more deterministic generation. If you choose the temperature to be zero it means that your generation will just be always deterministic every time you take the largest um um um um most likely token and then keep going on, right?
And of course you can also change the temperature to be smaller than um bigger than one, which makes the the distribution more softer, so that is you you focus more on the long tail and you can generate more um tokens from the tail, so you have more consistency and and certainty in your generation. Sometimes that's useful because you want to encourage some diversity in your generation and you you do that. Especially in RL, sometimes you have to do that. Cool. Okay. Sounds good. And sometimes there are also other ways of sampling, which is probably I shouldn't go into details. There are some notes in the lecture notes. So another way to do it is that you um this lecture right now I have three dimensions. This lecture is like the dimension is v right, like which is two hundred fifty or something. You can take top k and then drop all the other dimensions. So you sample from the the most likely k tokens, and you rename Normalize this solution. So basically, the first H tokens may not cover the all the probabilistic probabilities. So, but still, that's probably easier to generate because you don't want to generate two rare tokens. So you just drop everything below top K and then generate. Okay, sounds good. So I think I'm probably. And then okay, so once you have all, so this is the generation, and then for the training, I think I already can describe it a little bit. So when you do the training, you just compute the log likelihood, the negative log likelihood, the negative log likelihood, and you say this is a log. So if you have example x one up to x t, so the minus log p theta x one up to x t capital T is just the minus log the product of all of this, right?
P theta x one, p theta x two given x one, one so forth, and this is the minus the sum of the log of the t of x t given x one up to x t minus one, and then then you change this to log of the softmax of p one of this alpha, and here this x t is a particular choice of x t right, which is seen in the data. So that means you are looking at the x t index of this softmax. This softmax will give you the probability for every possible tokens, and then you are only interested in the probability see that particular x token x t. So that's why you look at x t index of that vector, and that's a single scalar. You take the log, and you take the sum over t, and that's your loss function. And then you optimize this loss function by, for example, some optimizers like SGD or Adam. We didn't discuss Adam in this lecture, which is the extension of the x SGD optimizer. But mostly, you can just call the optimizer as a black box to optimize the loss function. It's not actually not the last one, right?
Because this is sum from t to one to capital T, the little T and log. Okay, sounds good. Okay, so then in the next thirty minutes, I'm going to tell you what this this function f is, right?
Maybe what's hidden in this transformer?
What's the operation we have to do to compute the the the log is here, and it's pretty complicated. So it probably takes more than thirty minutes, but I will see how much I can do. So any questions before that?
Okay, so at a high level, this transformer has multiple layers, and so you start with e x one up to e x t, and you first apply a so-called attention layer, which I'll define, and then you apply the so-called MLP. So first, for the attention layer, the attention always takes in a sequence of vectors and output a sequence of vectors. So you output a batch of vectors at each position, output one vector, and then each of these vectors will go through an MLP. MLP means a multi-layer perceptron, which is just the two or three layers, two or three layer network, and each of these vectors will go through this MLP, and then you apply another attention. This attention will take in many many vectors, and and then output a sequence of vectors, and then you do the MLP. So that's the the high level structure of this attention, and eventually you have to output. Remember, eventually you have to output a sequence of vectors. So basically, you do enough of this alternation between these two, and eventually you have this. Uh, MLP and you output some vectors u one up to u t, and you alternate between attention and MLP. That's the highlight. And and I think we will focus most of our energy on defining attention, which is kind of like the most new thing, right?
Because MLP is just a two-layer neural network with some special kind of gating or kind of like some kind of like a special activation function. And another small thing is that the MLP are sharing the parameters and they are applying independent. So each of these vectors is processed by the MLP in a independent fashion. There's no kind of dependencies between positions when you apply the MLP. They are just various separate operations. So the only way that you are using um um you are kind of like fuse you fuse the input from different positions or different time steps is the attention. So the attention will look at all those vectors and process a lot and then output a sequence of vectors. And MLP is is very parallelized and parallelable and independent. Okay, so now let's let's define attention. This entire thing will be treated as a single system. So so so basically, when you define the architecture, you apply the auto differentiation. You you like each each whole thing as a as a gigantic neural network, yeah, and one. Yeah, but the auto differentiation still works. So so the auto differentiation, I think, as we discussed, if you remember, like it's kind of fundamental. It's claiming that if you have a way to compute your forward pass, as long as they are compositions of many many different components, you know, like a basic components, you can compute the backward pass the same fast the same way. So here is is indeed a composition of many many small arithmetic operations because as you see, the attention layer has many small matrix multiplications and the MLP has many matrix multiplications and you can automatically differentiate. Um, okay, so okay, so now let's let's talk about attention. So for attention, one thing to remember first is that the type signature is that you you input a sequence of vectors to the attention and then output a sequence of vectors. And and we're going to start with the so-called single-headed attention. So the single-headed attention is like this. So you first have um uh a input as a sequence of vectors, um let's say h one, in up to h t in, and and you want to output, you know, h. I'm trying to define the the type signature. So you output h one out up to h t out. And what you do is the following. So the first thing is you turn this input vectors into a set of query key and and value vectors. So so you define so basically you say h one in you multiply by a matrix w q. This is a weight matrix. This is a parameter. And by the way, let's treat all of these as row vectors, and then all the multiplication on the right hand side as opposed to the left hand side. So every vector is a row vector, and you multiply on the right hand side, you get another row vector. So so this h in let's say this is like a dimension d, r d. I say say r one by d, and this w q is in dimension. So this is r one by d, and this w q is in r d by um d h. D h is a parameter. It's a dimension two. Um, and and then the resulting thing is called q one, which is in dimension d h, maybe one by d h, depending on how you think about it. Um, and you can do this for every index, every time step. If you're only doing for time step one, you can do it for time step t. So you have q t. So basically, this gives you a vector, a sequence of vectors, q one up to q capital t. Each of them is in dimension one by d h. And this called these are called queries. And you do the same thing for key and values. So key. T. I mean, define them first. So it's the same thing you say. The HT in multiplied by a so called K projection matrix W K, and that's a K T. And the dimensionality is the same. So this is also in the same dimension, and the K T is in the same dimension, and the same thing for value vectors, not value functions. HT in times W V. So in some sense, maybe maybe this is the right time to talk about some of the intuitions, you know, for the attentions, which is not obvious because eventually just works. You know, like nobody really knows exactly how it works. Um, um, but the the right intuition is that you have to have some interactions between these inputs to make it meaningful, right?
Because if everything is independent, then you just are dealing with all the times independently. So that's why you have to have some um um interactions, and the interactions sometimes is about how you kind of like which tree or kind of pay attention to each other. So that's actually how the word attention comes from. So basically, when you are processing the maybe this vector, you probably should attend to or pay attention to some other vectors which are have relevant information, and which one you you pay attention to is what we are decide here using which this query and key and vectors. So in some sense, intuitively, the attention will what attention will do, which I didn't show you yet, is that you try to um for processing each of the vectors, you try to figure out which are the relevant vectors that has relevant information, and and then you pay actual attention to those vectors, and and then you compute the output depend on depend on those special vectors to some degree. So that's what we are trying to do. Um and and to kind of decide which one you pay attention to, you have to prepare this queries and key and value vectors. Um and how do you use them?
So use the query and keys as a way to decide which one you pay attention to. So basically, say suppose you have um the T projection, right?
I want to decide what the T projection to pay attention to. Um and and the way to do it is you say you compute the query Q T. This is the the query T projection, and you multiply the query with the keys at all the other positions. So maybe I will just draw it. So you have queries Q one up to Q T, and one of them is Q little T, and you have Q K one up to K K T. And you you multiply Qs with all the keys. So you have Q one times you know K one transpose, Q T times K two transpose, and Q T times K half to T transpose. Um and there is um sometimes there is a scaling factor here to kind of like normalize this in a way that makes it a little bit more kind of like well behaved, but I think for the moment you probably don't have to pay too much attention to the scaling factor. So the important thing is you are taking the inner product between Qs and each of the other keys, and this Q T transpose K Q trans K transpose is actually an inner product because Q is a is a row vector, and K transpose is a column vector. And you are taking inner product between these row vectors and the column vectors. Um um and you get one scalar out of it. So so this is um all of these numbers are real numbers, right?
So this is in R T. So you have T real numbers, um and then you take a soft max over these T real numbers to turn it into a probability vector. So then you say I'm going to do a soft max. So soft max of this whole thing. And now you get what you get. The this is a vector. The dimensionality here is R T, and each of this this this the the outcome of this soft max is a sequence of numbers, right?
And there are T numbers, and and all of these numbers are negative, and the sum of them is what?
So it's a probability vector over T positions, and if you see a large number, it means that. Let's suppose this number is big. It means that this number is somewhat big compared to other ones, and means that Qs and keys have like more higher inner product, right?
And it means that you should pay more attention to K K one, right?
So so basically, out of these entries, big it means that intuitively. QT needs to pay more attention to K1, and and that's what we reflect by the probability vector, because the the corresponding probability will also be high. And so I think let's call this. I guess I have notation here, so let's call this. Let's denote this to be PT1, PT2 up to PT capital T. So the so each of the PTi is each of the PTi is negative, and the sum of the PTi over i from one to T is equal to one. So this is the probability vector. And and if this I know this entry is big, it means that we should pay more attention to that particular position when processing the little T's query. And then how do you turn this?
How does this relate to the final outcome?
So finally, once you have this, then you say the the output at T's position, which is a vector, is using these probability vectors to weight the values. The values is V, which I will be defining here, VT so VT. So you say it's PT1 times V1 plus PT2 times V2 plus PTT times VT. So basically, if each of any of these probabilities much higher, then you are multiplying this high probability with the the corresponding value at that position. So basically, you are using the V. Right, if some if this number is very big, then you are using V2 a lot in the output at time T. So you determine by multiplying matrices, and the matrices will be trained. Uh uh like like yeah. How how do you attend to each other is also trained. Like so so you only give your template in some sense. Like how how you know like it may be that you know, like it could be that all of these are zero. You know, then you are not doing anything with that, and then your network the loss function will be not good, but it's still valid network. It just the loss is not good. So eventually, you are going to train the model to behave well, so that so basically the training algorithm will choose the right values so that this mechanism is doing the right thing. So um let's try to articulate. You know I think it's a good question, but let's try to articulate so that we can answer it. So what um uh you are so. Yeah. So I think you are asking why you need such architecture by not just only using MLP for example, right?
That's one way to phrase the question. I think um so if you are only using MLP in this way, suppose you remove all the tensors, you you only using MLP in this way, then there's no interactions between different positions whatsoever. So there's no chance this is going to work. Right, you are just applying separate networks for each of the tokens. There's no chance you can understand the the the the the dependencies, you know, the the meanings, right?
Like there's no chance like I I just give you individual words and you can understand the sentence. There's no chance, right?
So so you do have to um have a model that can read uh uh um all the tokens and and use and study their kind of correlations and combinations and so forth, right?
But um there are other possible possible ways to deal with that. For example, what if you use using gigantic MLP that takes in all of these, maybe you concatenate all of them and you take in one MLP, right?
So so um for example, you just concatenate all of these tokens vectors into one long vector and you go to a network. So there's several issues with that. One is that um you um will have way too many parameters because your sequence length may be sometimes millions. Then if you have a million by million matrices, your MLP is going to be way too big. Well, now the MLP is very small. It only takes in a vector for one position. It doesn't even depend on T. And another thing is that you're not only the the size of the MLP is big, but also the size of the MLP is also changes over um as you change the sequence length, which is also very undesirable. Because for example, if you train with sequence So that's a thousand, and then you test with sequence lengths of thousand one. Then you have to change your parameter count, which is not good. And and here, if you really look into details, you'll find out that the number of parameters doesn't depend on the sequence length. Yeah. Yeah. Exactly right. So so so you you know that you know if you have sufficient compute, you probably a decent MLP will work. If you have sufficient data, so you don't overfit, and you have sufficient compute, then there's some MLP that can actually solve this problem. But that's too expensive. So you need to prescribe some structure so that you can reduce the complexity, right?
And and this is the the the prescription we have. But you still have to leave enough flexibility. You cannot just say for example, one way to do it is you just say average. Then there's no parameters. You just prescribe too much, right?
You you dictate too much. Then that's not good. So it's kind of like in in structural balance between how much structure you use and how much flexibility you give the models to figure out. Yeah. Yeah. And any other questions?
By the way, there's actually in the history there was actually other ways to deal with sequence. For example, you can use a convolutional network, others, which is kind of like MLP. So you have use the use the MLP but with a specific receptive field, so that each time you only see a kind of like a window. I think there are still papers that so says that that's not a terrible idea. Actually, if you use a convolutional network, it actually can work to some degree. It's just not as good as this one, you know. And and and also you can see that there are also other, probably we'll talk about that later. So there are other variants of attention to that that similar stuff but have better efficiency. Anyway, so let's continue. We we are not done yet. Actually, this is only a single single-headed attention. You know, actually the real thing is a lot more complicated. Um, but let's see before I go into that. So I think you can see the notations here is very messy. So let's try to simplify it a little bit so that we don't have to always work with this messy notation. So one typical way that people simplify this is that you try to organize all the queries into a matrix. Recall that each of the queries a row vector, and if you put them all in a matrix, then this is a matrix of dimension T by D H, and you can put all the keys into a matrix, and you can put all the values in in a matrix as well. Um. Right. So and then you can write this H out. Um. The output vectors, you know, is a kind of like um, also the matrix is in some sense you do a soft max of the row of Q K transpose divided by C matrix times V. So here the basically Q K matrix, Q K transpose matrix is the the final Q I times K H transpose divided by C. So every entry is of this form, which is kind of like this guys, and then if you do a row soft max, that's corresponds to this soft max, and then if you multiply by V, it means it means this combination. I think you we probably we don't have time enough time to go into the details. Um, but you can double check. You know, it's just very basically algebra. It's just we are rearranging like a it's more more simple notations. Right. So this Q K divided by C matrix is inner product between the queries and keys, and then you take a row wise soft max to get the probabilities. So after the soft max, this matrix becomes um every row is a probability vector, and then you multiply that with the V to get the inner combination V. Um, and there's one thing that we kind of cheat a little bit here, which is that if you look at this architecture, then it's not true that the T output only depends on everything before it. There's no this kind of like a auto recursive kind of like properties, yeah, right?
Because the output times T. Depends on everything, right?
It depends on vt, capital t, even the last input. Vt, capital t, is a function of the last input, and so and the output at time t depends on the input, depends on vt, and it depends on ht. In so so basically, there's no kind of this dependency. So then, what you do is you say introduce the concept of masking to make sure that there's no this kind of like wrong dependency. So basically, what you really want to do is that you just in some sense for h little t, you want to delete everything after time t. So basically, I say before you have this pt vt plus pt tvt pt capital t v capital t. So basically, you say these dependencies are invalid for me, so I just don't have them. So basically, you just delete all of this, and and then you re-normalize these ps so that the sum of these ps are normalized to one, so that it's still a combination of combinatorial combination of the v1 up to vt. So that's pretty much what you do. So you you just say if the dependency is not good, you just in some sense mask them out. And mathematically, what you do, actually, also implementation wise, what you do is do this masking, which means that you which means that you add masking here. Maybe I use different color to so this. So you add masking here, different by the color. Okay. So you add a mask here, so that you can mask out the let me see, you can mask out half of the this matrix. So so you have like q1 k1 transpose, q1 k2 transpose, so on so forth, and q1 k2 transpose is not valid in some sense because you are looking, you are attending to something you should not attend. Like q1 shouldn't be able to see k2. So basically, you add this masking matrix. You what you add is that you add minus infinity. You add minus infinity here, and for q1 k3 transpose, you also add minus infinity, so on so forth. And then in the next row, you have q2 k1 transpose, which is good. You have q2 k2 transpose, which is good, and you have q2 k3 transpose, which is not good. So you add minus infinity here, so on so forth. So basically, everything here will be adding additional minus infinity. So basically, this upper triangle part of this matrix will be all minus infinity. You know, and then we'll by adding minus infinity, just mean that you you turn it into minus infinity, right?
And then when I when by minus infinity is the minus infinity, and then what happens is that in other words, it's kind of like you are just trying to add all of minus infinity to all of this part, and then when you add when you turn all of this into minus infinity, when you do the softmax, this part doesn't doesn't change doesn't like doesn't by this part mean everything after t, right?
So everything after little t, you you turn them into minus infinity, and after you do the softmax, they become zero. So because exponential to the minus infinity becomes like so so so becomes basically zero. So that's why in this p so everything after pt t everything after t will become zero, and the the sum of the rest will be one. So that's exactly what you how you achieve this because basically the combination of the rest of the terms doesn't matter because their coefficients are zero. Yeah, we are masking this because you shouldn't in this alternative model. You don't allow the output at time t to depend on anything that is after t, so you have to respect this kind of like sequential dependency. So you know, in generation, you you are not able to see the future tokens to generate the current token. So so every token only depends on everything before it. So that's why we have to mask out. Okay, we have. Yeah, we are almost there. Okay, so, um, okay, so this is the so-called single head attention, um, and we have to make this a little more complicated to even finish the attention part. So, then what we do next is that, maybe I see raise here. So, in some sense, what we're doing here is that we have h one in, and h t in, and we go through this single head attention. I guess in a note, I call it attention one h, and you get this h out, h one out, up to h t out. That's what we find so far. And in practice, you have multiple copies of this single head attention with different weight matrices. So you get another copy of the single head attention. Maybe I call this single head attention one. This is called single attention head attention two, and you have the same input, and then you get the another copy of the output. And here, that copy are not really literally copies because then it's not useful. So what's the difference between this and this is that the weight are the weight matrices is different. So basically, this w q, w k, w v for these two copies of the attention are different. So so so that's why they are not really literally copies. They are just the like you have another another version of it. And um, yes. So so in some sense, you should index this by something else because the output will be different. So maybe you should index this by, let's say, this is because this is the first attention. You say h one one and h t one, and this is h one two, h t two, and you also have another one, and you get h one three up to h t three. So so so you have so many copies. You have several sequences of vectors, but eventually you only want one sequence of vectors. So you have to combine them in some way, and the way to combine them is that you um, you concatenate these three things. So you concatenate h one one out, h one two out, one. I think how many each of this is called one head. So you have let's say in h and h head, so this is attention, and sub h. So you concatenate all of these vectors, and you get and then you multiply that with another projection matrix, and then this will give you the final h output at one. So that's the final output at position one, and you do the same thing for position two where you concatenate, uh, maybe this guys, this guys, and and then you do a projection, you get the output at position two. So in some sense, this is just give you more power to compute different kind of like um uh um kind of attention. In some sense, you know, I think actually in practice, if you, I think the intuition is that maybe each of these attention is doing single head attention is trying to pay special type of like uh is doing some special type of job. For example, maybe here is trying to find out which entity in the past passage matters to me, and here this is trying to find out you know which um um um what kind of the sentiment of the past passages uh and and which part of the sentiment matters to me. So so forth. So each of these attention probably pay attention to different type of information, and then eventually I will get the outcome of all of this information into one um vector, and you project it into um uh a single output. Um. No, no, they are they are all parallel. Yes. They are they are parallel, and then you aggregate. So and all of this is in one. And then so basically, in one of this, you have multiple parallel heads, and then next one you have another number of parallel heads. I think the number of heads is maybe on the order of like maybe at most a hundred, I guess. Um, um. Let me think about this. I think something on that order. I think, yeah. You know, it also depends on model size. Of course, I think the biggest one. I don't know. I don't know whether a hundred is is for a smaller size. I think maybe for like a hundred billion parameter models, you have about a hundred heads. Yeah More closer to linear t, because there are other ways variance of transform that can reduce the dependence on t. Um, I think after in the next Wednesday we will discuss a few um things that can reduce the dependence on t a little bit. Um, exactly how we do it, we don't really know in some sense because the there are a lot of papers that propose different approach. Um, with I I don't know exactly which one is used the in in open source like um um and another thing is that what is the memory usage here?
So the memory usage in as as at the first side you have to t square memory because this matrix is of course t square memory, but that actually can be reduced because you don't have to actually compute the whole matrix one by one and then do the multiplication with v. So in some sense, to some degree, you can do some part of this matrix and multiply with part of the v first, and then you don't have to save that part of the matrix. You can compute the second part. So and and the idea is called flex flexension problem you have heard of, which is a way to reduce the the memory um um footprint in this system. Um, so yeah, I think that's the but but you know like some some of this kind of like ideas do have trade off because we reduce the um um for some we have to change the architecture to make the dependence on t square better. For some we discuss some of this um um let's say so if you change the architecture the dependence on t is better, but your architecture less expressive and then you may lose some performance to some degree. Okay, sounds good. So I guess you know um I think we have um you know Chris has talked about linear model which is kind of like you have model which is parameterized by theta um and input x let's just recall notation so that we are not saying partial notation. I think Chris is using almost exactly the same notation, you know it can be exactly same because sometimes it's not linear model, right?
So um but for for linear model we call that you know it's a linear model and the model is kind of like this right?
You have theta h x which is equal to theta one x one plus theta two x two up to theta d x d where x is the input which is a d dimensional vector and theta is the parameters which are also d dimensional vector. Um and what's the non linear models?
What does that mean?
You know, generally non linear just means that at least this form is non linear, um then is non linear and the and here we are mostly talking about non linear parameters because it's non linear in the data is not non linear in some sense. So suppose you have something like theta one x one square plus theta two x two square, so of course the d x d square. I think we didn't talk about this kind of models, you know h you know because we skipped them in some sense in the new version. But this is not sufficiently linear to be interesting because you can just define new data which is the square of old data. So suppose you say I'm going to replace this by z one, I replace this by z two, and then still become a linear model. And now we are not learning, we are not we are not trying to find out what the you are trying to learn the theta theta i, right?
You want to figure out the parameters of theta i. You don't really have to figure out there's no learning on z i, right?
So whether z is z i x i square it doesn't really matter, right?
So that's why this is not exactly we are talking about. Even though some people would say this is a non linear model, but this is not exactly we are talking about. We are talking about cases like you have theta one square, for example, x one plus theta two maybe you know just an example. Something like this, right?
Whenever non linear model is right, at least non linear is is called non linear model. Okay, so um of course you know the real model we are talking about is not like this. This is just an example. Um, so the model we are talking about is new works which is more structured and more you know you know you know specialized way. Anyway, so but before talking about new works, let's talk about how you learn non linear models. Um, and what's kind of the framework?
Um, this is in some sense mostly just recalling what Chris was talking about with a little bit twist about you know the model. So basically the model can be a general function as opposed to um linear function. So um let's just quickly recall some of this. So let's say you have a dataset. I think Chris probably mentioned the concept of dataset which is the finding a data point with um a pair of input and output. So here let's talk about supervised learning, but wait um you know in other cases you know In this lecture, in this whole course, we are trying to use this notation where the superscript is for the indices of the examples, and the subscript is for the the mentioned coordinates. Right?
So when write x sub i, this is the first coordinate of the vector x i, and when superscript is is index is indexing one particular example. Right?
Yeah. I think that is the units of time. Let's just only talk about the matrix classification. Basically, you just have to define the loss function the same way as we have done for for linear models, except you replace the models by nonlinear functions. By the way, I think in most cases you don't have to think about the superscript i because you know this is just index examples, right?
If you remove, if you say I'm going to have one example x1 and x1, then everything with the superscript still works. Maybe I should just write in notation to simplify a little bit. You know, so let's say you have so in this case the y, you know, I can write y superscript i for the i label, or I can just use y to indicate one label. Let's say suppose one label in the matrix classification, you have it's a discrete choice of labels, right?
One to k. And here I guess you know you can say you have model let's use h bar, which is mapping the input x so the x is the R D. Think of this as image, think of this as some kind of labels for the image, and then the which is mapping the input from to k numbers, and these k numbers are often called logits. This is the same logits in the in the linear case. So and then you say the which is logits the predicted probability of each class of each class for this example x for each class, this class j is the this kind of exponential form, right?
You look at the j's this is the j's output, right?
Index j means that this is the j's output of this k-dimensional vector, and then divide by the sum of the exponential of all the this is the so-called softmax. So this is the so-called softmax. Sorry. So this is the so-so-called softmax function of this h, and then so basically you say so basically your model is like this. So this this is your model. So p of y is equal to j given x is this is my parameterized model for the prediction parameterized theta, and then I need to compute the log likelihood. So I'm going to use negative log likelihood, which is also what we have discussed before. By the way, here this is also log likelihood model if you assume there is some Gaussian probability distribution. I didn't really define go through that calculation. So if you think of that your model your probability model is something like a Gaussian to the h theta, and then you compute the log likelihood and the Gaussian then it's the same square error. Here I'm just going through more details. So I define my probability distribution, I compute log log likelihood on the data. So this is the minus log p of y given x and the my parameterized model, then this is the minus log of this thing. So oh sorry, I think what's happening here is that this is the so this model specifies what the probability for every choice of j, but now I have a particular choice of j which is equal to my label y, so that's why I'm including my y. So this is exponential h theta bar x. This is for the particular label I'm given the y index the the y coordinate of that um logit vector divided by the the denominator is the say just the sum of exponentials. Oh, I think I have some typo here. Um, and this is just called the the entropy loss. I think if you look at the matrix classification case, this is the so-called cross entropy loss. You can have many different forms of it, but this is the the cross entropy loss. Where the you can just write this h theta bar. So this is logits, this is the label, and you can plug in cross entropy loss. If you pay attention to implement it, I think you can find the model which is just called cross entropy loss and give it to In put you can compute the loss, but of course the underlying math is this. Okay, this all sounds almost familiar to some degree. Okay, any questions?
Um, is cross entropy?
Oh, so so so what's the second part of the question?
Yeah, I think this is the cross entropy between the, um, the the probability distribution defined by h theta x. Okay, so I think here's the thing. So if you, so the question is why this call cross entropy loss. Um, I think if you, um, how do I how do I write it in some sense like?
I think if you have a, I think this distribution that's called um maybe p theta. Okay, so and the, um, I think this cross entropy loss is actually equal to the cross entropy. Between the true um I guess you have to take um, if you take the expectation of this of cross entropy loss. Of this uh h r and y, expectation of y from the true distribution, from the true label distribution whatever you call, and then this is equal to cross entropy. Between uh this distribution p theta, and the true distribution p star, which is something like you I guess the cross entropy just the is is actually the probability of one distribution times the log of the probability of the other distribution. Um, yeah, I think the derivation is probably a long. Especially if you read in this language, it will become more long. But but this is fundamental. I think actually probably I have it in the lecture notes. I I forgot exactly. Um, yeah. Any other questions?
Why what is the one hot?
You know, in in my case, you know, in my notation, y is just a discrete variable, so that's why I you can see I have h theta bar theta x, in x y. I y is like two, three, or four, okay. So um, but if you can also include write y as one hot vector, which is the same thing, right?
So like then my thing needs to be a different way. Then this is going to be the inner product between this vector times the one hot vector. Yeah, but but here I'm just using y as really a discrete choice between one to k. So um, can you repeat the question?
Sorry. Okay, so the question is why for the regression case, it also is not like not um log log likelihood, right?
So I think let me try to see why can do it. I think it should be easy. So I think the the modeling. So if you make the model assumption that if you assume, so if you assume that the model assumption is that y is generated from, say h theta r theta x plus some Gaussian noise with absolute from and zero maybe sigma square something like this. Um, then the likelihood of y given x is, you know, I think one over I guess this is changing my memory of this something like this. X minus so y minus h theta x square over two sigma square I guess something like this, and then you take the negative log likelihood. So then this is the log of p y given x is equal to the some constant. You take the log, you get a constant which doesn't really matter, and you got the log of this, which is over two sigma square y minus h theta x square. So then it it's kind of like this, right?
The scaling doesn't really matter because this like it doesn't change the optimization function fundamentally. Any other questions?
Okay, cool. So um, and and in this case, you know, here I only have like one example. Eventually, the average of many examples. So. I guess maybe just let me write here. So if you continue here, so eventually your loss, you know, the real real loss is something like the the final loss is the average over all the examples. You take the cross entropy loss of each of the examples x i and y i. So and and each of these functions is function of theta. So basically, your total loss is also function of theta. And now the next task is to optimize to find theta that makes the loss smaller, right?
The same I think we also introduce a lot about gradient descent. You know, like um um um um Newton method. So this always to optimize things. So here you can use Newton method because the function is not convex. So that's why Newton method is not guaranteed to work. All actually no methods can guarantee to work for all non-convex functions, but stochastic gradient descent seems to be a good idea. So just to quickly recall, you know, you can use optimizer. Um. So so optimization. I think one option is you take gradient descent. So so let's say suppose you have a loss function. Let's just summarize all the cases as this. Just be abstract. So let's say each example has one have the loss j i of theta, and you have average of these loss functions. Gradient descent means that you just say I'm gonna ignore the structure here. I just treat this as a single loss function. I I take the update theta is update to. I think use this notation to denote update something like eta times gradient of theta. So this is gradient descent. Um. And how do you compute this gradient?
You you compute the gradient of each of the loss, the gradient. Uh. Sorry, you compute the gradient of each of the loss with respect to parameters and you add them up. But that's gradient descent. And for stochastic gradient descent. So what you do is you say you first sample say b um. Let's see. So you sample. I guess there you know maybe let's say the most vanilla version of it is you sample one item you sample j from one to n. You know formula. And then you run the you know. Um. So of of course there's a for loop. And you sample and then you say theta is update to theta minus eta times the cost of the example j the cost loss of example j theta. And the the kind of the justification here is that if you think about what this is, so the difference here is that here you compute the gradient of the average loss, and here you only compute the gradient of one single example, the loss on one single example. The justification here is that you say even though this is one example is not actually the loss loss of the whole gradient, but actually is the expectation of the the gradient the per example gradient, and your randomness comes from j right, which is sampled from you know formula from one to n. This is equal to the gradient of the the average gradient the gradient of the loss function. So so in some sense this quantity is a approximation of this quantity, and and the approximation is that this quantity in average is equal to this. It just sometimes it might it's you know it's it's never approx you know let's say suppose this quantity is like in this direction and this quantity just the it can be any direction, but if average out all the possible points, you get the average direction go. So in some sense you have like a much larger noise right?
So if you do gradient descent, you it's kind of like uh I guess maybe I use two different colors. So suppose you do gradient descent. This is the gradient of the the average loss, and if you stochastic gradient descent, it's like stochastic you may go here, you may go here, or you may go here. Maybe you sometimes may even go in the reverse direction, but in average out of this blue one, will average out in the same direction as the black one. And and so and then if you have a trajectory, so suppose the black one goes like this, the blue one because it's stochastic you have noise right, so it will go look like this way. Maybe something like this. Maybe like even but in the average is kind of roughly tracking the gradient descent. Of course, how much this tracks, you know, how much is two things tracks over time is is. Very complex kind of like matter. Like it depends on how large the the subset is, and how you know kind of like depends on many kind of like subtle things. But conceptually, you you should expect that the blue one somewhere attract the the the black one. You know, the the blue one in average is exactly equal to the black one in one step. But every sum times can be amplified, sometimes every can be shrunk. So it depends on the situation. I really want to try to some degree. Um, I think you can use gradient as well. Okay, but I think I think I got the question. You know, this is I think the it's kind of the the the the question is really about why you use stochastic gradient. In what cases you use stochastic?
That's a great question. So why you even consider stochastic gradient?
The reason is that computing the gradient is very costly each time because you have so many samples. In could be you know in twenty fifteen it's kind of one million examples. In twenty twenty sixteen now I think it's kind of like you know like one trillion tokens. Right, there's no chance you can compute all the gradients. Even computing one gradient is very costly. Like computing all the gradients. So that's why you have to find efficient ways. So so that's why you want to have a sampler which is less costly. And and maybe just so and also another reason you can do this is that computing the full gradients sometimes is actually wasteful because maybe it's the case that all of these individual gradients are actually they are correlated, they are highly correlated, right?
So there's no reason to compute each of them to get understand which direction to go, right?
So you just only need a sub some. It's kind of like every example tells you to go that way. Why you have to compute all of them, right?
You just need a subset subset of them. And another thing is that even this is not entirely true. In many cases, like you don't really have to be very precise in this in this dynamics. It's kind of like even you have some mistakes, you know, will be corrected later. So some maybe another thing we need to think about is for some cases like for some very convex function, right?
So in some sense, like one way to do it is you do this way, but sometimes if you kind of like overshoot, you know, maybe you you go in the wrong direction, still eventually you will go down. It's how do I say this?
Like you know, basically even you have some perturbations. This is not exactly the right example, but even you have some perturbations in your dynamics, then over time because you are every time you you have to share a gradient, eventually those kind of perturbations got kind of like washed out by the by the by the shared force in some sense. Yeah, I think that's a that's a great question. So one of the theory for why stochastic is is that it can work better on non-convex functions and maybe make let me first explain what you said, but but actually is not entirely right. So um so I think um um so basically suppose you have a non-convex function in one D, let's say, right?
So like this. So if you start here, you you keep going down. This gradient is going down here in some sense. You go to the direction which is going down here the fastest in some sense, right?
So you just go here, you get stuck here because you can never leave this this place, right?
Here the gradient is zero, right?
The the curvature is zero, um and the um and the and the I think what you're trying to say is that if you do stochastic gradient, because there's noise, maybe even though you won't go this way, maybe the first step you just because the noise you go the other way and jump to a local minimum. Um, I think this is plausible, but generally speaking, you know, um, of course the theory here is kind of very limited, but I think the community don't believe that this is the case. This is why stochastic gradient is helping. I don't think people believe that stochastic gradient can help you to jump out of local minimum. Um, however, on the other hand, we don't even believe that there are many bad local minimum. So so in one D, it's actually very easy to have local minimum, but in high dimensional cases, it's not that trivial to have that many local minimum. So so so um I think at least one in actuality, one of the remarkable theory, of course nobody really knows exactly whether they are right, but the the common let's say hypothesis or hypothesis consensus in some sense is that there are very limited number of bad local minimums in this in this high dimensional function. Or or maybe there's even no bad local minimum. All the local minimum You just randomly try to go down scales, scaling down here in two D, right?
It's probably you'll probably going to go back to one of the parking lot, which is one of the global minimum, right?
So, like, you know, but in two D, I think it's a bit easier to have a local minimum. You probably go back to and you got stuck somewhere. That's possible, but you know, you always find some way of going down. You always find some way of going down eventually. I think, you know, not, you know, I'm just giving analogy. It's not exactly in two D, but in high dimension, it's more likely to always find a way to to go down down here in some sense. Why?
Yeah, I think I don't think there's a very simple way to say it. Um, I think um, it's not it's not universally true, right?
So, like, so in some cases, um, I think maybe the you know if I I can only use one two dimensions, I think it's kind of like just in high dimension, it's always there's always some way to like local minimum is your hessian at that point is all all directions positive semi-definite. That's very hard thing to satisfy. So, like, so, so, so you find the you you find the stationary point where the gradient zero, and you get the hessian. The hessian is kind of like um always have some direction which is negative. So, um, and I think I I was thinking about how to formulate this, you know, like so, for example, if you think about the gradient zero is already a pretty strong constraint. But suppose you you already have like suppose you have a dimension function and you add the constraint that each of the dimension of the gradient is zero. That's already restrict yourself a lot. And if you add another thing, which is the hessian positive semi-definite, I think that just makes the the number points much smaller. Yeah, and in some cases you can compute. In one my papers, I think we try to compute the number of local minimums by using the um I think in um in differential geometry there's something called Hessian formula. I actually not only my papers, but many other people's papers. So in some cases you can compute, you can count, and you find out that all the local minimums are just those number of local minimums is the same as the global minimum. But um, um, but generally it's very hard to count. Any other questions?
Okay, sounds great. So, um, okay, so going back to here, so so that's the algorithm people do. Um, of course, practically speaking, you don't also just only sample one sample each time because sample something one sample, you know, sampling you know using all the samples is too inefficient because you have to compute all the gradients. Um, doing one sample the downside is that it's too noisy, right?
So sometimes you have like a a two D, right?
So you you have a few examples, and so that you can um um you can be a little less noisy. And another thing is that um which probably we won't go into here. But another thing is that um we have one example your generalization is too too low. You know, not it's not always the case, but in some cases using too few examples make your generalization too low. Some if you have like ten thousand GPUs and you just only compute one example one gradient each time, then most of the GPUs are idle. So so that's why you need so small batches in these sense. Also, um, I think it's called it's also still called small batches. With some kind of like small batches, um, it's I think I D just means like both version with size one or um a little bigger batch. So when you have a bigger batch, is you know you can imagine what it is. Basically, you just say I'm gonna um with batches um I guess just call it I D. So I think you just change the algorithm to be sample say B um examples, um one to n, and then you say uh say uh it equals say uh minus eta gradient. Okay, sounds good. So and okay, so this is just the general introduction to the framework for the supervised learning. Um, um, and actually, in some of the unsupervised learning, the same thing you define a loss function for each example, and you average the only question is when you have unsupervised learning later, um, that will cover you don't have the y. So but still, the kind of the framework is the same. You define the loss functions, but the loss functions involve y, and then you have average loss functions, and you run the optimizers. Um, you can choose different type of optimizers. Um, so um, Right, so this whole formula, you know, works for every function h theta, and h theta bar. I think I use bar just because it's a little bit, you know, with without bar, just the I just want to distinguish from the regression case, but they mean the same thing. And so, and the question, so question one is how do we find h theta, and question two is how do we compute how to compute the gradient, the gradient J on each of the example. And once you know how to compute the gradient for each of the example, you can compute the gradient for all the examples, all batch examples, um, averaging the gradient for every examples. So um, and that's called um, so this is called back propagation. The algorithm for this is called back propagation or auto differentiation, which is the topic next lecture. And in the rest of this lecture, I'm going to define a bunch of h theta that people are using. Um, mostly for like more complex cases, like regression cases, like supervising, um, um, um, and of course for language case, you're going to have even more complicated um, nonlinear functions like transformers. Okay, so okay, so that's the plan. Um, I'm going to start with you know the very basics, like neural networks. So, so we're still using kind of housing examples. Um, I know that this example is probably too old, like two years old. So, so let's say suppose you have like two more days. Um, the the the introduction of like nonlinear models. Suppose you have like size and and the this is price, right?
That's housing price prediction. So you know, if you look at the real data, most likely something like this. Maybe some I don't know exactly where it looks like, but at the beginning, it doesn't increase that much as the size goes up, and then eventually becomes linear. Let's say this is the case we see in data, right?
So you know, if you use a linear model, you're going to fit something here, then you're not be accurate enough. So that's why um, you probably have to have a nonlinear model, which is something like this. So you want your model to be able to output curve like this. Of course, you don't really know which line is this. It could be like a you know a curve without seeing the data. You can have multiple kind of curves like this, but at least you should allow the model to generate curve like this. I suppose to only generating a linear function. So and the and what this function is, right?
So this function, um, so is actually of this form. So this blue function is actually the form is max of um say double x plus b, which is a linear function, um, and zero. I guess you know especially if you suppose this is zero, then you just the maybe I say this is max double x plus zero plus c. So max of double x plus b is kind of like a function like this. And then if you with zero, sorry, this one is like this, and then if you plus c, you can shift it above the um above or below a little bit. So basically, just cut everything that is below zero, which makes sense because you know for housing price, you know it cannot be negative, you know it has to be positive. Um, so yeah, okay. So and um and so this actually this introduces a primitive that is used very often in deep learning days, which is this function um, which is max of t and zero. So I think in mathematics people call this people write this something like this, but but for us we just call this relu. I think this is the the standard in machine learning. So this function is called rectified linear units, um, which is literally this function where you have linear functions on the positive plane and then zero on the other side, so zero and then linear, and linear with slope one. So this is a very important primitive, and then you can write this function as you know this function can be written as relu of t maybe plus c something like this. Sorry, relu. I say this one will be written as relu of double x plus b plus c. So basically, you introduce so you know if if you don't have the relu, then it's basically a linear function, but if you have the relu, then you somewhat change the shape a little bit. And um and also this relu is often called activation function. I think the reason for the word activation is because in um there's some kind of like connection to um biological networks. I I In biology, you know, like in your brain, in your brain, the neurons can, like, how the neurons fire up is not a function of the stimulus it receives from other neurons. So, and the the it's not exactly this, but it's something like this. So, when it's passing through threshold, you got the neurons got fired up, and um, um, and and below that threshold, the neurons just doesn't do anything. So, so that's why it's called activation if it's fired up. So, but that thing in biology, I'm not an expert there, but you know, based on my um knowledge of the the threshold, the function is is not exactly like this. Sometimes it's like it's sigmoid, and that sigmoid was used at the very beginning of deep learning. Um, um, and but um, but then later on, people realized that this is actually um, this value is actually a bad idea for, for neural networks for various computational reasons, which we'll probably discuss, uh, right?
Anyway, so for now, I think that's kind of like um, just remember this concept. So, so actually, we already defined uh, uh, one-dimensional neural networks. So basically, this is a neural network with one one-dimensional input and a few parameters. So that's our neural network. So then the rest of the the the for for ten ten fifteen minutes, we're just gonna generalize this to multiple um, higher dimensions, you know, like and and you apply this multiple times, and that will make a more complex neural network, and then we're gonna even build more on top of that. So, um, okay. So next, what we do?
So, so this is only one-dimensional input, right?
And so now let's try to do a higher-dimensional case. So suppose you have data with dimension d, and then how do you kind of like imitate this and make it non-linear?
So the way to do it is that you say I have x in d, and then I have a parameter w, which is also dimension d, and often this is called a weight vector, and actually eventually we weight matrices. So so that's why weight and matrix, you know, associated together. No weight vector is is a matrix, and then you also need to do another number b, this is called bias, um, and then you define your model like this, um, say value of W transpose x plus b. This is inner product between two vectors, and you get a scalar out of it, and then add another bias, which is still scalar. So the output of this is scalar. And so then basically we have like um, of higher-dimensional inputs and one-dimensional outputs. This is this is actually called one-layer neural network. So basically, apply this value um, um once. So and the and now um, let's try to kind of like stack multiple um. So this is one neural actually because you you apply one activation function is one neural, and then you have multiple neurons. So and one of the typical examples I use to give you know maybe this is not exactly the best example, this is for example, suppose you think about the housing price, right?
So it probably depends on a lot of intermediate features. Suppose housing price depends on, for example, um, maybe okay, maybe I'll give you a few examples to slow down a little bit. Let's suppose you have this cases where x one is the size, x two is the number of bedrooms, and x three is let's say the zip code, and maybe x four is is maybe the, I don't know what else for x four is, I don't know what it means, but anyway, this is your raw input, right?
So so um, maybe you can think of like there are some intermediate things that that actually matters more than this raw inputs. Let's say you have intermediate quantities that you quantities like for example, say um, walkability, right?
How how easy to walk around the house, right?
Maybe there's another one which is the school quality. So these are in some sense variables that are not present in your original data, but they matter a lot to the the price of the house, right?
And and each of those, you know, maybe let's call this a one, this is a two. Each of this is actually a function of x one up to x four, which can become quite some non-linear function. So so you can imagine a one is probably some kind of like non-linear function of you know maybe value of some of the linear combinations of this thing, and you can imagine maybe a two is also some non-linear function of this of um. Maybe I say say that I'm using notation here for a sloppy way to give example. You know, finally we're going to define things more formally, and then you can say, oh, maybe my final y should be a linear combination of these guys. Maybe I have another parameter, say, the y a one plus say a ten, a two something like this. So then this way you you can of like stack two layers on top. We have two layers, and you stack another linear function on top of it. So sometimes you apply this recursively. So so that's kind of like the fundamental idea. How do you kind of compose?
So basically, you apply this nonlinear functions. You know, you apply a linear function, you apply some nonlinear functions, and then you get some outputs, and then you apply another linear version, and you can probably apply another nonlinear function here, and you keep compose. So that's the that's the rough idea. But let's try to make it a little more formal. So that it's probably more obvious. Let's see what's the right way for me to erase this stuff. I think we can probably start from the left. So one of the ways that people I used to draw this kind of things, you know, at least ten years ago was that they draw this kind of like inputs like this. Suppose you have x one up to x d, and then you say I have some intermediate variables a one up to a d, maybe a m, and then each of the intermediate variables depend all of this, and they have some dependencies here. So let's say a m depends all of the input variables, and you just draw this kind of edges, and then you can say I have another layer of intermediate variables, and they connect to each other. So these drawings are you know, once you know what they are talking about, the drawing is very useful. But when you don't really know what they are talking about, it's kind of like confusing. So what I really mean here is that you know, the way you do it is you say first let's say let's do only one layer, maybe just the formula. So we have x one up to x d, right?
So then let's have a one, the first intermediate variable is the value of w one x one plus b one, where w one is a vector. Even though I have an index one, but it's the first first vector w one transpose, and x so x x is here. X is a dimension vector. W one is a vector, so the inner product together you get a scalar, and you add the scalar b one here. That's how you get the a one, which is kind of like what I know there like a one. So I use w just because I think people use w and b to distinguish the the weights between these and the biases, and then you have a two, which is the value of w two transpose x plus b two, where w two is R d and b two is R. And you have a bunch of this. Let's say you have up to a m. Let me see what m is my notation here. It's very easy to make a notation in these kind of cases. Yes, a m is value of w m transpose x plus b m, and that's how you define a one up to a m. And each of the a i depends all of the x i in this particular way, and then you can say I'm going to apply another my output will just be a linear combination of all of these a i's. Let's say the output is some linear combination of the a's plus another plus another biases. So here is the thing: how do I write a linear combination of the a i's?
You know, I can write something like say the one a one. So and so first, but if I always kind of explain these two complexities, so what I'm going to do is I'm going to write a vector. Let's say w transpose a plus another b, but you realize that you know when I write this, you know this w needs to be distinguished from other w's. So and that's why I think I'm going to use this two to say that this is the w on the second layer. In some sense, these are the w on the first. So there's another. So all of these a i's are combined to h say x, and that's why people write this is the w on the second layer, and this is the w on the first layer. So I'm going to write another bracket w one something like this to distinguish the the first layer and the second layer. So and this w two superscript bracket two is a vector of the same dimension as a, not of the same. This has x anymore because you have to compile, you have to multiply with a. So this is the dimension rn because m and this here, and b is always a scalar. So that how you define two layer network. So you have a lot of matrix, and you can see this notation is kind of messy, right?
So like you know, you probably you know, when you have like so many superscript um on subscripts at some point it's kind of like too messy. So that's why for most of the cases, you know, some cases you have to live with these notations. Um, so but in most cases you can simplify, you know, further. You say actually if I write everything as as matrices and vectors, I don't have to really think about each of the um details. So basically I just write a as a vector in this is a vector in rn. You can write as value of matrix w times x plus b, and here w and b are a little bit different. Uh, okay, so w I guess I need to write w one and b one. So this is a matrix w one. If you want to look understand the correspondence between this notation and this notation, so w one is this matrix of dimension m by d, and each row is w one one transpose up to w one transpose. Right?
So you put all of these row vectors because once you transpose by the way all the vectors are supposed to be column vectors if you don't do any transpose. You know, if you really understand this, you should try to understand the like at least when you know in many situations, right?
When like uh in many cases is good to keep in mind like whether this vectors is a row vector and column vector. So all of these are column vectors, and then you can transpose this into row vectors. So this is a row vector, and you put all of them stack them into a matrix. Um, and and then this b one is just the vector of all the scalars. But in many cases actually don't have to necessarily remember this correspondence. You just remember this is one, right?
Is you have a vector multiply a matrix to get a vector, and then you get a vector bias, and you take value and twice. That's probably important. So value applied to vector t is the same as you apply and twice. So so in many cases you just say oh one matrix multiplication one layer is just multiply a matrix by the bias vector and apply value and you get a vector out of it. So you don't have to necessarily understand all the details. You know, sometimes you do, but most cases this is you don't have. So um and and if you count how many parameters are there, right?
This is m by d, this is the dimension. So m by d, right?
Because the in input dimension is d, the output dimension is m, and the matrix turns the d dimension input to all the m dimension outputs. That's why you need m times d parameters, and then this guy has m parameters. So so the total parameters, parameter count is m d plus m. So d is the input dimension, m is the output dimension, and this is our output dimension. This is b one, yes. And this b one is this b one. So any questions?
And I think you know, eventually actually some many of these details don't matter if you don't care. So for example, you can even just write this like for example, you can just say oh, I I even simplify notation here. You can say um I think you can say let's just say this is a matrix multiplication. So let's say you have a let's say you have a matrix multiplication with w b. So w is the weight matrices, which is this w, and b is the um the the bias, and you apply z, and and this is just equals to w z plus b, and and and then you can say I'm going to apply a sigma. Sorry, let's say. Maybe just before I talk about that, let's I think I already alluded to it. So this relu here can be used also to other nonlinear activations. So you can replace so the relu with you know some other nonlinear activations, and all the activations are functions that maps R to R, so it's always a scalar function. And but you know you can apply this to vectors, and so I'm applying it, applying and so on. And other activations could be like sigmoid, um, sigmoid looks like this. Um, you know, let me let me just take some time to now define the equations, so you can read the notes to see the exact equations. There's some there's also tanh which looks like goes to zero and that's so all of these activations are generally increasing functions, but not always the case. I think there's also leaky relu. You know there's bunch of different activations functions. For example, leaky relu looks like now this is actually used much. Um, if you want to see what's the use the latest neural network, you have to check the lecture notes. I just added a few which I don't have on my notes, but there's also functions that there's you know anyway generally is kind of like some almost increasing functions, and often on the on the left side of zero is close to zero, but not exactly. So some of the one that just oh this is called the relu. Um, let me now to write. I think the the definition of it is the relu's definition is just just in case you can the exact equation, so it's not exactly nonlinear, just it dips a little bit. I think it doesn't go above zero. Like this, tricky speaking. So it dips a little bit and then goes up. So exactly why use any of this is kind of like a little kind of like magic. Like um, I think you don't. I think people seem to find that using some kind of some smooth functions is is actually useful. So relu is actually not necessary, right?
Because it's not smooth enough. But on that I don't really know um exactly um there's like sometimes just the but there's also some sometimes just the trial and error, yeah. Um, so okay, so that's the activations, and then so now I'm trying to um compact, you know, I'm trying to kind of have have more abstract versions that you don't have to deal with all these notations. Um, um, so then once you have activations, you can define this, right?
You can this is just the sigmoid w z plus b. This is abstract version of that, and then you can compose many times. You can say I'm gonna apply another one, so my model can be like say apply a multiplication on top of this sigmoid, and then right, so w b z, and then apply another thing, apply another thing, and so on so forth. And each of these is one layer, and then if you apply another multiplication sigmoid, another layer, and then if you do another one, it's another layer. So number of layers is defined as pretty much as the number of multiplication. Yeah, but here one thing I'm a little bit curious about is that I didn't tell you what the parameters here in this multiplication. So technically, if you want to write this formula, you have to say oh this is w b and this is what it is still using w b or some other w some other b. So often times you use different w and different b. It's the the the the same sigmoid, but the the variables are different variables. So then you should then you have to introduce the bracket again. So I say this is bracket one b one, and then here the parameters are bracket w bracket two b two, and so on so forth, and then index each of them to fully specify the network. Which matrix?
So this matrix. Yeah. So okay. Yeah. So what these matrices are, so the only requirement is that you need to make sure that the matrices is compatible, right?
So um, so your input right, right?
Z. Oh yeah. Okay. So let's suppose the input z is. Say dimension D, then you can only apply a matrix, you know, with so to make WZ make sense, you have to make sure W is of dimension something by D, right?
And something is what you can choose, you know, almost freely. Of course, not all options are the same. Eventually, in terms of the relative for the compatibility issue, perspective, you can choose them arbitrarily. And let's suppose you the first layer you choose, let's suppose you choose, um, let's you choose matrix multiplication, say, um, W one, B one, Z plus. And suppose this W one is M one by D, so that means that you map this D dimensional vector to M one dimensional vector. So I say, so this output is going to be M one dimensional vector. And then next time, and if you apply sigma, it's still M one dimensional vector. And next time you apply the matrix multiplication, you have to make sure that the the input dimension of the matrix to be compatible with M one. So basically, if you do another one, say W two, B two, then it's required that W two is to be something M M one dimensional, um, and and something the output dimension is still what you can choose freely, but the the input dimension is not. And then next, and you have to do this, you know, again and again. So once you have multiple dimensions, so basically the, like I maybe the easiest way to think about is that you have a matrix, right, times say Z plus B. So this dimension is what I call the input dimension because this dimension you multiply with the dimension Z, and this dimension is output dimension. But the output dimension becomes the input dimension for next round. That's why you need to make sure all the dimensions to be compatible. That makes sense. And many cases I think, you know, the the the the original input dimension is uh something you cannot change, right?
So like that's fixed, that's based on detail. And afterwards, you don't change dimensions that often. You typically use the same dimensions for quite a while. Maybe you change it once or twice, something like that. Okay. And you know, like if if just in case you read some of the old papers, you know, which I think even this is still actually happening sometimes. So like this, like they they just draw, you know, people go more and more abstract. They say I have input X, and then apply a matrix multiplication, and then apply another maybe sigma, and then apply another matrix multiplication, and so forth. That's how people actually draw this network, you know, without specifying all details. And and you can even merge these together to call it MLP. So sometimes you just draw this, you have X, and MLP comes out to a perception of it's encapsulate um one or two layers of this of few of these components. And people say MLP. I think sometimes is one matrix multiplication, one sigma. Sometimes is two matrix multiplication, so on so forth. Um, I think in most cases you get smaller dimensions. The intermediate dimensions become smaller, and that's often because the input dimensions often very big. For example, for language, it's kind of like your initial, uh, the the vocab is kind of like the token, you know. I guess I mean Japanese had a little bit, but like your initial dimension could be like 250k or something like that, and then you you you shrink it to like 2k and then keep using 2k, yeah, something like that. And for vision, I think the same thing because initially you have a RGB encoded kind of like image, you have three channels, three RGB, three numbers in each is like 256 by 256 or maybe depending on the resolution, and you have to shrink a little bit, um, yeah. Um, okay, what else?
I think one thing is quite relevant. I think that's still useful, so probably should tell you about it. So um, let me write something here. So but eventually, you know, like you can find many components like this. Most of these components are matrix multiplications, activations, and I'll tell you a few other components that are still largely being used, you know, in different cases, um, um, and and and the um some of the very more than cases in um, using like more more than components like attention, using um um language, I'm not talking about those, you know, you know, in large language models, so so these are some um so. Well, the the component residual network, or just the residual connections. Uh, if you think about the the components, so the residual connection is like this. So instead of defining, instead of using, keep using this this kind of things, the residual connection is kind of like, that's called residual z. And this interesting thing where you have two applications of this alternating between, between summation and and activations, and then you add your input again back to it. So one thing that is at least the definition, it means that you don't change the dimension here, right?
You because to make this comparable, your input dimension and the dimension are exactly the same. Um, and of course these two matrix operations are not using the same weight matrices. Not the right exactly what they are, but they are not using the same matrices. Um, and the and this is called the residual block. And the this was introduced into to benefit in this very famous residual network paper. I think the kind of the the rationale was that, if let's suppose, but this z doesn't have to be original raw input. This could be one intermediate layer, and then you apply this z, you apply this residual block, and then you you can actually apply many many residual blocks. So eventually, the this is called residual block, and eventually the residual network, the residual network is like, you know, with some approximation, is kind of like you apply a lot of residual nets many times. Um, and at the beginning you need to kind of do something maybe to change the dimension, because you cannot work with the original dimension. So you first change the dimension to some dimension of the intermediate dimensions, and then you apply many many residual networks, and maybe you have to apply one more at the end to change the dimension again. But all we're speaking is application many many residual blocks, and that's called residual network. Um, and the original motivation was that, um, while you apply this, let's suppose suppose we believe that at some point you are already getting getting close to your labels. I suppose at some point z is already approximate to y. Sorry, y is the target, and z is intermediate variables in some intermediate, uh, um, uh, like layers. Then, um, you probably should model the differences explicitly, because you are kind of close. You just probably should use networks to model the the the only the differences. So basically, you probably just model z minus y as some residual, I mean y minus z, sorry, uh, so y minus z as some residual of z. And and if you believe in that, like, sorry, not residual z, y minus z should be some kind of like network, say something like this. Plus, right, yes, right. So if if this is your uh your belief, then if you move z to the other side, then you have y minus z equals to sigma and and z plus z, and you can see that this is kind of like almost the same as here. The only difference is that they found out actually you probably didn't use one layer to model the difference. You should use two layers to model the difference. So so they say oh let's do two of this, and then the same thing you just have. So this is just a story, not like and there's any formal proof here. Um, and and actually people have tried to, you know, ten years ago people have tried, maybe seven years ago people have tried to see how many blocks here. You know, can do two layers, three layers, L layers, and eventually two or three layers are the right, the best. Um, also for reasons we don't fundamentally understand necessarily. Um, yeah, so that's residual network. I thought, yeah. So so so so suppose this is the model function. You model only the differences, then, the so this implies that they are the same. So like which one is helpful depends on what you what you believe in. So but but somehow if you if somehow believe that it's more beneficial to model directly the residual, then you you you do this right, and then you change and you rearrange together. That's that's nothing I'm saying. Nothing profound. And there are other kind of like explanations for why this works. You know, some some of the theories, and they try to analyze the optimization perspective of different modeling, right?
So basically, if you don't add z here, versus you add z. You know, in some cases, you can argue that adding z will make the optimization easier, make your optimization more well-conditioned, including some of my papers. You know, I think I think there's some the truth there as well. It's just it's very hard to find out. You know, it's like you have many explanations for for it. It's it's very hard to know exactly which one is the right explanation. Yeah. Maybe let's see. I have seven minutes and any question. There is no, you know, I got a question. What's the question?
But like, maybe I see the intuition could be that oh maybe this is already small, then maybe I should directly model that. I I supposed to model the the original label. So it's a simpler task. The original label, you know, predicting original label is it's very hard. But if you predict the difference, probably easier. So that's why you model easier tasks with a two-layered network. And that's pretty much it. Like there's no nothing more formal than that. Yeah. Um, let's see. I have seven minutes. I think let's see, should I attempt to talk about two things?
Okay, so maybe let me quickly talk about layer norm, and then I can see whether I have time to talk about convolutional neural network. So layer norm is also something that people still use in large language models. So of course it's like different form these days. So the original layer norm is the following. So basically, still it's it's a component-wise. So like it's a it's a block. So you have some input that's called z, and and this component is trying to normalize z in some way. And the way to normalize it is that you say you first turn z into some normalized version of z, which is z minus mu one over sigma one, minus z minus mu and sigma. I say z m dimensional z z one. Not bad. I should I should read the note. I should follow the notes more closely. I think this is uh the same. Yeah. Maybe I should even use. Yeah. So let's say. Okay, I don't remember why you had, but let's use had. So this is how you normalize. Where mu hat is empirical mean, which is the average of all the z's, and sigma hat is the empirical sigma squared, is empirical average of the. Let's do the division. So this will empirical normalize to one, right?
So you can see it's pretty simple. And and then it's not done yet. And then you turn this into sometimes you find people find out oh actually mean zero and one is not that great for whatever reason. So they say oh let's be flexible. Let's also add um a flexible tunable parameters beta and gamma. So let's turn the next step is turn this into beta plus gamma z one minus mu hat sigma hat, where beta is a is a tunable par beta is a variable they can tune and gamma is a variable they can tune. Um and so basically this make a mean beta and and standard deviation gamma. So that's the original layer norm. And then later people realized actually you probably don't have to do this. You just the so this is the layer norm. And now I think there's something called RMSE norm, which is even simpler. You don't um subtract the mean anymore. You just divide by sigma. So you um so you just say you do z is turn into z one minus sigma one sigma hat, say z and minus sigma hat. And sigma hat is also different. Sigma hat is only the um the average um is only kind of like the you're only averaging over like the you don't have the you don't subtract here. So you just try to make the um so sigma hat is something like um z z I sorry z squared. And and then you still do the beta thing, right?
And then you turn this into beta plus the one over sigma. You know why why this is kind of like more better or not?
You know, I think it's mostly empirical. So basically, here you don't really want to make the. Let me think about this. Wait, sorry. I think you still need gamma. Sorry, I keep gamma. Keep the beta. Keep the gamma. Sorry, right. So so gamma is kind of parameter. So basically, this thing I don't have to care about the shifting mean. I don't really make mean zero, but I I still care about normalizing in some way. But I still leave some degree of flexibility gamma, which you know is not really normalizing. I still allow to to normalize something that I I decide later in my training, right?
So so this is kind of like the the magic here. It's not like you you try to force it to normalize to one standard deviation. Why you actually say I first normalize to one and then leave gamma parameter there, so that in the training I can normalize something. Um and um, I think let me see. I don't have much. So um, so one benefit of all of this is that you make kind of like um, scaling invariant in terms for both parameters and the data. So what that means is that if you multiply, so maybe let's say you call this whole thing called like Z. Let's just say, right. So like let's say suppose this whole operation is called layer norm. So you apply Z and you you get the layer norm Z, and you can really you can find out that there's a scaling invariant, which means that if you apply a scalar, let's say you you multiply a scalar say alpha to Z, and you get alpha Z, but alpha Z is actually equal to alpha Z. So basically, no matter how you multiply the input, you don't really change it. Um and so that's why no matter how you multiply your um your matrices or how you multiply your input data, you never change the output. Um unless you change the the some of the parameters like beta gamma, which is you know at that time you are not supposed to change them. So so basically, this makes the the model more scaling invariant. Um and in some cases it helps um stabilize the model for example. At least intuitively, you know, um with some degree of approximation, you know, there there at least in in the past when people tune the oldest models, it happens that sometimes you apply the the the uh the layers many times and then the output becomes bigger and bigger, and the and it becomes like really big, it's like into the twenty or something like that, and then um uh everything just blows up. So when you normalize every time, then you you never got into issue, and also makes the training a little more kind of like easier, so that you don't have to you know be afraid if you don't do something right, and suddenly everything just blows up, and now like the scaling doesn't really matter much. Um but there's also something here because when you have okay this might be a too advanced comment. One side effect of this is that when the when the forward pass is scaling invariant, but actually the the gradient is not scaling invariant still. So so so so some of this issue is pushed into the the position, so um which is um much more complex matter. Um anyway, so I guess um let's see. So I think the the only thing I didn't cover is the convolutional network. I think it's in the lecture notes. So um if I spend one minute on it, I think it just means that um instead of multiplying of matrix, you actually multiply um a topless matrix, not sure how to describe this word. Like so basically the matrix with some structure is really like um so so when yeah instead of having this matrix convolutional network, just basically means one matrix that's kind of like like the like this. The non-zero is only like this, and and it's actually the same as the non-zero. So you you you shift this vector here and you put it here and you put it here and put it here. It's actually called like um a feature in some sense, like so so this matrix all the other parts of the matrix is zero and only this what I drew here is kind of non-zero, and also the shared parameters. So basically, this vector is the same as this vector. So you apply kind of like a shifting invariant kind of like feature to each to um to the data. Um and it's called convolution. Of course, the real thing is a little more complicated. So when you have two D and so and so forth, becomes um complicated. But in lecture notes we have that. But convolutional network is not that um used that much, except for very hard formulation cases. Like even for which