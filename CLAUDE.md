---
title: CLAUDE.md — このボルトの運用ルール
tags: [schema]
created: 2026-07-29
---

# このボルトの運用ルール

Claude / Cowork がこのボルトを編集するときに従うルール。Andrej Karpathy の "LLM Wiki" パターンをこのボルト用に翻訳したもの（出典は [[01_Claude×Obsidianで第二の脳を作る]]）。

## 3層構造

フォルダには数字プレフィックスを付け、用途順に並べる。プレフィックスはWikilinkに影響しない（このボルトのリンクはすべて「ファイル名のみ」の最短パス記法のため）。

- `01_raw-sources/` — 生の資料。記事・PDF・文字起こしなど。**不変**。読むだけで書き換えない。詳細は [[17_raw-sources について]]
- `02_wiki/` — Claude が生成・維持するページ群。以下に分類する
  - `02_wiki/summaries/` — 資料1本ごとの要約ページ。内容のジャンルで数字プレフィックス付きサブフォルダに分類する
    - 既存カテゴリ: `01_AI`（AIエージェント設計・PKM等の概念・理論）、`02_ツール`（個別OSSリポジトリ・製品の紹介、リンク集など理論色の薄いもの）、`03_セキュリティ`（セキュリティ関連）、`04_論文執筆`（学術論文の書き方・図表作成・研究手法などの作法）
    - `01_AI/` は更に4サブフォルダへ細分化: `01_エージェント設計論`（Harness/Loop/Graph の概念・理論）、`02_Claude Code実践`（使い方・ワークフロー・MCP・スキル）、`03_エージェント運用・ガバナンス`（長時間運用・ファクトリ・品質保証）、`04_LLM・RAG・基礎`（LLM基礎・RAG・モデル選定・プラットフォーム）
    - `01_エージェント設計論/` は更に4サブフォルダへ細分化: `01_概念と入門`（3層/5層フレーム・前提概念・入門解説）、`02_Loop実践`（Loop ロードマップ・実戦原則）、`03_Graph実装`（Graph 実装マニュアル・ロードマップ）、`04_Graph判断と検証`（Loop↔Graph判断・検証器・一気通貫・リポジトリ）
    - `02_Claude Code実践/` は更に4サブフォルダへ細分化: `01_PKMと第二の脳`（Obsidian/PKM/第二の脳）、`02_ワークフローと発注`（計画と実行の分離・spec駆動・時短Tips・発注の型・オーケストレーション）、`03_MCPとスキルとアーキテクチャ`（MCP/Skills・ハーネス理解・学習教材・5層/6層実行エンジン・コンテキスト設計）、`04_マルチエージェントとモデル運用`（サブエージェント・チーム化・モデル/effort/コスト・コーディングエージェント実践）
    - `02_ツール/` は更に4サブフォルダへ細分化: `01_エージェント運用インフラ`（ワークスペース・隔離・通信・リモート）、`02_MCPとスキルと開発ワークフロー`（スキル化・MCPプロキシ・PR・テスト）、`03_RAGと検索と観測`（知識グラフ化・視覚RAG・トレーシング）、`04_ツール集とリスト`（汎用ツール・OSS代替リスト）
    - 該当するカテゴリがなければ新しいサブフォルダを作ってよい（既存の最大番号の次から採番）
  - `02_wiki/concepts/` — 用語・概念ページ
  - `02_wiki/projects/` — プロジェクトの状態ページ
  - `02_wiki/daily/` — デイリーノート
  - `02_wiki/howto/` — 運用・使い方メモ
- `index.md` — 内容志向のカタログ。全ページをリンク＋一行説明で列挙。Ingest のたびに更新する
- `log.md` — 時系列の追記専用ログ。Ingest / Query / Lint を実行するたびに1行追記する
- `99_to_delete/` — 削除候補の退避先（この環境からはファイル削除できないため移動で代用）

## Ingest（資料の取り込み）

1. `01_raw-sources/` に新しい資料を置き、読む
2. 要点を抽出し `02_wiki/summaries/` に要約ページを作る（frontmatter は最低限 `title` / `tags` / `source` / `created`）
3. 関連する `02_wiki/concepts/` や `02_wiki/projects/` の既存ページがあれば、内容を反映して更新する。矛盾があれば明記する
4. `index.md` に新規ページへのリンクと一行説明を追記する
5. `log.md` に `- YYYY-MM-DD Ingest: <資料名>` を追記する
6. 触ったファイルを全部提示する

## Query（質問への回答）

`index.md` から関連ページを探し、開いて根拠付きで回答する。良い回答は `02_wiki/` へ新規ページとして書き戻せる。`log.md` に `- YYYY-MM-DD Query: <質問の要約>` を追記する。

## Lint（定期点検・週1回目安）

`02_wiki/` 配下の全ページを読み、次を洗い出してレポートを書く。

- ページ間の矛盾
- 被リンクゼロの孤立ページ
- 繰り返し言及されるのに専用ページがない概念
- `01_raw-sources/` の新しい資料から見て古くなった記述

レポートは `02_wiki/lint-report.md` に書き、`log.md` に `- YYYY-MM-DD Lint: <要約>` を追記する。

## 命名・frontmatter規約

- ファイル名は **`NN_ノートのタイトル`** 形式（`NN` はフォルダ内の通し番号2桁）。新規ノートは各フォルダの既存の最大番号＋1を採番する
- フォルダ（`01_raw-sources/` / `02_wiki/` / `99_to_delete/` および `02_wiki/summaries/` 配下のカテゴリ）にも数字プレフィックスを付ける
- frontmatter は最低限 `title` / `tags` / `created` を持つ。種類に応じて `source` / `author` / `status` / `due` などを足す。`title` はファイル名（プレフィックス含む）と一致させる
- 日本語ファイル名・日本語本文が標準。技術用語は無理に和訳しない
- Wikilink は「ファイル名（プレフィックス含む）」で張る。Obsidian の「最短パス」記法なのでパス不要
- 削除はしない。不要になったら `99_to_delete/` へ移動するだけにする（この環境からはファイル削除ができないため）

> [!warning] リネーム時の注意
> CLI からファイル名を変えた場合は、**全 `.md` 内の Wikilink と frontmatter `title:` を手動で追従**させること。Obsidian アプリ内のリネームなら自動更新されるが、CLI リネームでは検知されない。
