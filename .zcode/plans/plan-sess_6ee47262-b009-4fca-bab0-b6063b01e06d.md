## 概要

`02_wiki/summaries/01_AI/` 配下の2フォルダを更に細分化する:
- **`01_エージェント設計論`（15件）→ 4サブフォルダ**
- **`02_Claude Code実践`（18件）→ 4サブフォルダ**

前回（2026-08-01）の細分化手順（バックアップ→再採番→全Wikilink/title追従→index/CLAUDE.md更新→log追記）を踏襲。影響範囲は約25ファイル・約160箇所の Wikilink 更新。

---

## 01_エージェント設計論（15件）→ 4分割

| 新サブフォルダ | 旧番号 → 新番号 | 件数 |
|---|---|---|
| `01_概念と入門`（3層/5層フレーム・前提概念・入門解説） | 02→01, 04→02, 06→03, 08→04, 15→05 | 5 |
| `02_Loop実践`（Loop ロードマップ・実戦原則） | 05→01, 09→02 | 2 |
| `03_Graph実装`（Graph 実装マニュアル・ロードマップ・入門） | 01→01, 07→02, 12→03, 13→04 | 4 |
| `04_Graph判断と検証`（Loop↔Graph判断・検証器・一気通貫・リポジトリ） | 03→01, 10→02, 11→03, 14→04 | 4 |

※Graph系8件（01/03/07/10/11/12/13/14/15のうちGraph系）を実装(03)と判断・検証(04)に2分割。15_Graph入門は概念と入門(01)に（入門解説のため）。03_一気通貫パイプラインは Graph 判断・検証ではなく Graph 実装寄りだが、3層統合の実装チュートリアルで「一気通貫」の性質上、判断と検証（04）に隣接配置。

---

## 02_Claude Code実践（18件）→ 4分割

| 新サブフォルダ | 旧番号 → 新番号 | 件数 |
|---|---|---|
| `01_PKMと第二の脳`（Obsidian/PKM/第二の脳） | 07→01, 09→02, 11→03, 12→04, 18→05 | 5 |
| `02_ワークフローと発注`（計画と実行の分離・spec駆動・時短Tips・発注の型・オーケストレーション） | 03→01, 05→02, 06→03, 17→04 | 4 |
| `03_MCPとスキルとアーキテクチャ`（MCP/Skills・ハーネス理解・学習教材・5層/6層実行エンジン） | 01→01, 02→02, 13→03, 14→04, 16→05 | 5 |
| `04_マルチエージェントとモデル運用`（サブエージェント・チーム化・モデル/effort/コスト・コーディングエージェント実践） | 04→01, 10→02, 15→03 | 3 |

※04が3件と小さいが、主題（マルチエージェント・モデル運用・コーディング実践）は他グループと明確に異なるため独立させる。16_6層アーキテクチャは worktree隔離・マルチエージェント要素もあるが、全体が「ハーネス内部構造の解説」のため03（アーキテクチャ）に配置。

---

## 実行手順

### 1. バックアップ作成
`_prefix_backup_20260802/01_エージェント設計論/` と `_prefix_backup_20260802/02_Claude Code実践/` に、現状の2フォルダ（計33ファイル）をコピー。前回パターン踏襲。

### 2. 新サブフォルダ作成とファイル移動・再採番
8つの新サブフォルダを作成し、各ノートを新番号でリネームして配置。同時に各ノートの frontmatter `title:` を新ファイル名へ追従（全33ファイル）。

### 3. 全 Wikilink と参照の追従（約160箇所・25ファイル）
以下のファイル内の `[[旧ファイル名]]` を `[[新ファイル名]]` へ全件書き換え:
- **`index.md`**（見出し `#### 01_エージェント設計論` `#### 02_Claude Code実践` 配下を新サブフォルダ構造へ再構成）
- **`log.md`**（過去の Ingest 履歴行の `[[...]]` を全て新名へ。前回踏襲）
- **出典ページ群**（`01_raw-sources/*.md` の「要約は [[...]] を参照」行・本文中の関連参照。08_LOOP vs GRAPH出典 などハブ的存在を含む）
- **他の要約ノート群**（03_エージェント運用・ガバナンス、04_LLM・RAG・基礎、02_ツール、04_論文執筆の各ノートの「関連」節）

### 4. CLAUDE.md の規約更新
行19「`01_AI/` は更に4サブフォルダへ細分化: …」を、新サブフォルダ構造（8サブフォルダ）へ書き換え。

### 5. log.md への Restructure エントリ追記
`- 2026-08-02 Restructure: 01_エージェント設計論(15)→4分割・02_Claude Code実践(18)→4分割…` の形式で追記（前回の2026-08-01 Restructure エントリと同形式）。

---

## 検証
- 新サブフォルダ8つのファイル数合計 = 33件（元と一致）
- `index.md` のリンク先が新ファイル名へ更新済み
- `git diff --stat` で変更ファイル数を確認
- 被リンク最多のハブノート（08_Agent Harness[12被リンク]・08_Context Engineering[12]・02_LOOP vs GRAPH[8]・09_Loop Engineering[8]）のリンク切れがないか重点確認

リネームは一括で実行後、Wikilink追従を grep で網羅的に行い、漏れがないか検証します。